{
	"mcpServers": {
		"laravel-boost": {
			"command": "docker",
			"args": [
				"exec",
				"-i",
				"${ZENTRIA_BACKEND_CONTAINER:-erp-app}",
				"php",
				"artisan",
				"boost:mcp"
			]
		}
	}
}
