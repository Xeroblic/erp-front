// Re-export del mapa canonical de rutas de atributos usado por el editor
// Ahora `requiredAttributesByType` toma su fuente de verdad de
// `attributesFieldPaths.ts` (las rutas usadas en los sections del editor)
import ATTRIBUTES_FIELD_PATHS from './attributesFieldPaths';

export default ATTRIBUTES_FIELD_PATHS;
