# Bitácora — 07-09-2026

## Temas del día

- PR #191 — correcciones tras revisión (selector de clientes compartido)

---

## Tema: PR #191 — correcciones tras revisión (selector de clientes compartido)

**Rama:** `feat/cotizaciones-selector-clientes-compartido`

- **[fix]** `CreateCustomerSaleModal`: se agregó una validación cruzada en el schema de Yup que exige al menos uno de `billing_company` o `contact_name`, igual que el `hasUsableName` de `SubsidiaryCustomerSalesController`. Sin esto, un alta con RUT + email pero ambos campos vacíos creaba un cliente que el backend descarta de toda búsqueda con `strict=true` (el default de `fetchCustomersOverviewThunk` y `fetchCustomersListOverviewThunk`), dejándolo inalcanzable desde la interfaz. El test cruzado se extrajo a una función de módulo (`hasUsableName`) para que `this.createError` no dispare `react/no-this-in-sfc` al quedar anidado dentro del componente.
- **[test]** Se ajustó el test que antes guardaba con ambos campos vacíos (ahora completa Contacto) y se agregó un test que confirma que el envío se bloquea, sin llamar al backend, cuando Empresa y Contacto llegan vacíos.
- **[fix]** `CreateQuotationModal` y `EditQuotationModal`: se reemplazó el fallback `?? 1` de `subsidiary_id` por `?? 0` (falsy) y se agregó una guarda explícita en el `onSubmit` de cada modal (mismo aviso que `useQuotationsManager.createQuotation`), para que el código deje de contradecir lo que el PR y esta bitácora afirman sobre no defaultear a la subsidiaria 1.
- **[fix]** `CreateCustomerSaleModal`: segunda pasada de revisión — el bloqueo de `nombre-utilizable` era silencioso (`Input` no pinta `invalidFeedback`, sólo el borde rojo) y el rótulo del campo Empresa decía «(opcional)», lo contrario de lo que exige el schema cuando Contacto viene vacío. Se quitó «(opcional)» del rótulo y se envolvió el `Input` de Empresa en `Validation` para que el mensaje "Ingresa Empresa o Contacto" llegue al DOM, no sólo el borde.
- **[test]** Se agregó una aserción sobre el texto del error en el test que bloquea el envío sin Empresa/Contacto, y un test nuevo que confirma que el rótulo ya no dice «(opcional)».
- **Referencia:** [PR #191](https://github.com/Xeroblic/erp-front/pull/191).
- **Estado:** Segunda ronda de correcciones aplicada en la misma rama; `typecheck`, `test:related` de los archivos tocados, `prettier:check` y `eslint` sobre las líneas nuevas en verde. Pendiente de push/actualización del PR (autorización explícita).
- ZF-104 — Título de pestaña y encabezado semántico en Cotizaciones.

---

## Tema: ZF-104 — Título de la página de Cotizaciones

**Rama:** `fix/zf-104-titulo-pagina-cotizaciones`

- **[fix]** `CotizacionesAdmin` montaba `<PageWrapper name='cotizaciones-admin'>` sin `title`. Como `useDocumentTitle` compone `` `${title} | ${name}` `` y `theme.config.ts` define `projectTitle`/`projectName` como cadenas vacías, la pestaña mostraba literalmente `| cotizaciones-admin`: un separador huérfano seguido del slug interno del componente. Ahora usa `title='Cotizaciones' name='ERP'`, el mismo par que `ClientesVentasDetalleView`, y el título queda `Cotizaciones | ERP`.
- **[fix]** El encabezado visible era un `Badge` estilizado como título (`<span>`), así que la página no exponía ningún `h1` al árbol de accesibilidad. Se reemplaza por `<h1 className='text-3xl font-semibold'>`, replicando el patrón de los otros 28 `h1` del repositorio. Verificado que ni el header, ni el aside, ni el footer, ni `Card`, ni los wrappers de layout emiten otro `h1` o `role='heading'`: no queda encabezado de nivel 1 duplicado.
- **[refactor]** Se retira el import de `Badge`, que quedó sin uso en el archivo.
- **[test]** Nuevo `CotizacionesAdmin.encabezado.test.tsx` con dos casos: `document.title` y presencia del `h1` accesible. Validado por reversión: con el fix revertido ambos fallan, y el primero reproduce el defecto textual (`expected '| cotizaciones-admin'`).
- **Decisión — `isProtectedRoute`: evaluado y descartado.** El punto 2 del issue pedía evaluarlo. Se agregó en una primera pasada y la auditoría independiente lo invalidó, con evidencia que se reverificó: (a) `AppInitializer` ya despacha `userMeThunk()` y `obtenerPersonalizacionThunk()` una vez por sesión y ya redirige a `/login` cuando no hay `access`, así que el beneficio declarado ya estaba cubierto app-wide; (b) `authSlice.ts:248` pone `isAuthenticated = false` ante **cualquier** rechazo de `userMeThunk` — red, 500, timeout o `CanceledError`, no sólo 401 — de modo que un `/perfil` transitoriamente fallido expulsaría al usuario al login con el token todavía válido; (c) el ref `profileLoaded` es por instancia y sin TTL, así que sumaba un `GET /perfil` por visita. Efecto neto: sólo riesgo. La ruta ya gatea `view-quote` vía `AuthorityCheck`.
- **Decisión — delta visual aceptado por el dueño del ticket.** El `h1` hereda el color de texto por omisión, mientras que el `Badge` lo pintaba con el color de tema del usuario (ámbar por defecto) y agregaba `px-2` más una animación de entrada de GSAP. Se descartó la alternativa de paridad exacta (`<h1><Badge>…</Badge></h1>`) para no conservar el `Badge` que el ticket pedía reemplazar. El criterio «se ve igual que antes» no se cumple de forma literal: es una desviación consciente, no un descuido, y el contraste sobre fondo claro mejora.
- **Alcance:** solo Cotizaciones. Quedan fuera los otros `Badge`-como-encabezado del repositorio, el `" | "` colgante de las páginas que pasan `title` sin `name`, y el hecho de que las acciones de escritura de la página usen `Button` plano en vez de `ProtectedButton` (contrario a §5 de `CLAUDE.md`, preexistente).
- **Deuda conocida no tocada:** `PageWrapper` redirige con `<Navigate to={loginPage} />` sin `replace` ni `state.from`, a diferencia de `ProtectedRoute`. No aplica a esta página tras descartar `isProtectedRoute`.
- **Estado:** `tsc --noEmit` limpio; suite completa 117 archivos / 809 tests en verde; `check:eol` LF, `prettier --check` OK y `git diff --check` sin hallazgos. Lint del archivo tocado: sin hallazgos nuevos contra `develop` (mismo desglose por regla); los preexistentes caen fuera de las líneas que el diff toca, y el archivo de prueba no aporta hallazgos.
- ZF-99 — Tercera revisión del PR #188 y realineación tras la fusión de #189
- ZF-105 — Pagos diferidos: la fecha de emisión ya no se precarga con hoy

---

## Tema: ZF-99 — Tercera revisión del PR #188 y realineación tras la fusión de #189

**Rama:** `feat/zf-99-estado-candado-docking` (PR #188)

- **Revisión (3.ª pasada):** los dos bloqueantes de la 2.ª quedaron cerrados y verificados —
  `docking.schema.ts` ya no cita el `aria-required` retirado, y el comentario de
  `DockingExtrasSection` más el cuerpo del PR declaraban la dependencia real con #189. Contrato del
  backend reverificado en `feat/technical-review-scoring`: `CONDITION_LOCK_AREA`, `options` siempre
  publicadas en v1 y v2, regla `nullable` del PATCH y `COMPLETION_REQUIREMENTS` sin entrada
  `docking`. La hidratación al reabrir una revisión se comprobó en `Step2FullReview` (spread de
  `initialData.details`, sin lista blanca).
- **Hecho nuevo:** con #189 ya fusionado (`66807163`), `SelectionCard` dejó de exponer
  `role='radio'` y pasó a `<button aria-pressed>`. El merge simulado de esta rama sobre `develop`
  dejaba **3 de 6 pruebas** de `dockingLockArea.test.tsx` en rojo, y el PR figuraba `CONFLICTING`
  por las dos bitácoras.
- **[chore]** Rama rebasada sobre `origin/develop` (`66807163`); conflictos de
  `bitacora_trabajo/03-09-2026.md` y `04-09-2026.md` resueltos conservando los temas de ambos lados.
- **[test]** Las cuatro consultas de rol hijo de `dockingLockArea.test.tsx` pasan de `role='radio'`
  a `role='button'`, el ajuste que el propio PR ya tenía anotado como pendiente para este momento.
  El contenedor (`role='group'`) y el helper `isMarkedRequired` no cambian.
- **[docs]** El comentario de `DockingExtrasSection` describía a #189 como pendiente y afirmaba que
  en `develop` los grupos seguían siendo `radiogroup`; ambas cosas ya son falsas. Ahora describe el
  estado vigente.
- **Verificación:** `typecheck` 0 errores; `test:run` **117 suites / 818 pruebas** (818 porque #189
  sumó 2 en `develop`); `dockingLockArea.test.tsx` 6/6; Prettier sobre los archivos del diff
  aprobado; `git diff --check` limpio.
- **Estado:** rama realineada y publicada; PR #188 aprobado con label `needs-testing`.

---

## Tema: ZF-105 — Pagos diferidos: la fecha de emisión ya no se precarga con hoy

**Rama:** `fix/pagos-diferidos-fecha-emision`

- **[fix]** `useDeferredPaymentForm` deja de precargar `issue_date` con la fecha del día al crear un documento: arranca vacío y obligatorio, para que el usuario escriba la fecha real de la boleta/factura. `due_date` ya no se calcula hasta tener una `issue_date` válida (los efectos de autocálculo existentes ya devolvían cadena vacía ante una fecha vacía/incompleta).
- **[fix]** El `DateInput` de "Fecha de emisión" en `CreateEditDeferredPaymentModal` limita `maxDate`/`maxYear` a hoy (antes usaba el mismo tope 2100 que "Fecha de vencimiento"): una factura no puede emitirse en el futuro.
- **[test]** Nueva prueba que verifica que el formulario arranca sin fecha de emisión y sin vencimiento calculado, y que éste se completa recién al ingresar una emisión válida. Se ajustaron las pruebas existentes que asumían el default de hoy (`useDeferredPaymentForm.test.tsx` y la integración `CreateEditDeferredPaymentModal.integration.test.tsx`) para setear explícitamente la fecha de emisión.
- **Verificación:** revertido el fix puntualmente para confirmar que la prueba nueva se pone en rojo (defecto recurrente #4 de `pr-readiness`); restaurado después. `test:related` (129/129), `typecheck` y `lint:changed`/`eslint` sobre los archivos tocados sin hallazgos nuevos (los de `CreateEditDeferredPaymentModal.integration.test.tsx` son deuda preexistente, mismos hallazgos antes y después, sólo corridos de línea).
- **Alcance:** cero cambios en backend, según lo definido en la card. No se agrega `created_at` a la UI.
- **[feat]** Se agregó la columna **"Fecha emisión documento"** a `DeferredPaymentsTable.tsx`, segunda columna (a la derecha de "N° documento"), con el mismo formato que "Vencimiento" (`formatDeferredPaymentDate`). Se sumó `'issue_date'` a `DeferredPaymentsSortKey` y su caso en `getSortValue` (ordena como texto `YYYY-MM-DD`, igual que `due_date`). Se ajustó `colSpan`/skeleton de 8 a 9 columnas y el `min-w` de la tabla de `1150px` a `1300px`. Este ítem de la card ZF-105 no estaba cubierto por el commit inicial del PR #193; se agrega ahora para completar la card antes de publicar el PR.
- **[test]** Nueva prueba en `DeferredPaymentsTable.test.tsx`: la fecha de emisión se muestra formateada y el encabezado dispara `onSort('issue_date')`.
- **Verificación:** `test:related` sobre el componente y su prueba (10/10 verde); `typecheck` 0 errores; `eslint` puntual sobre el archivo — mismos 2 hallazgos preexistentes (`import/no-named-as-default` en imports de `Table`/`TableCardFooterTemplateV2`, confirmados idénticos antes del cambio vía `git stash`), ninguno nuevo; `prettier --check` y `git diff --check` sin problemas.
- **[fix]** Revisión del PR #193 — bloqueante cerrado: el tope de "no emitir en el futuro" sólo vivía en el calendario del `DateInput` (`maxDate`/`maxYear` alimentan `inRange`/`disabledYear`, pero `handleTypedChange` nunca consulta `maxD`), así que tecleando la fecha se guardaba igual un documento con emisión futura y `due_date` autocalculado desde ella. Se agrega el test `issue-date-not-future` sobre `issue_date` en `types.ts`, con el mensaje exportado como `DEFERRED_PAYMENT_ISSUE_DATE_FUTURE_ERROR`. El día se calcula en cada validación (no al importar el módulo) para que una sesión abierta durante el cambio de fecha compare contra el día correcto.
- **[test]** Dos pruebas nuevas en `deferredPaymentForm.test.ts`, con fechas relativas al día de ejecución para que no caduquen: rechaza la emisión de mañana y acepta la de hoy. Ambas verificadas por mutación — quitar el `.test` deja en rojo la primera; cambiar `<=` por `<` deja en rojo la segunda.
- **Verificación (2.ª pasada):** `typecheck` 0 errores; `test:related` sobre los 4 archivos de producción del diff **12 suites / 138 pruebas** en verde; `eslint` puntual sobre `types.ts` (limpio) y sobre `deferredPaymentForm.test.ts` (1 `import/order` preexistente, confirmado idéntico vía `git stash`, sólo corrido de línea); `prettier --check` y `git diff --check` sin problemas.
- **[docs]** Cifras del cuerpo del PR corregidas contra el diff real. Antes de esta pasada eran **+108 / −13** (+91 / −13 sin bitácora), no los +83 / −15 declarados ni los +112 / −13 que citaba la revisión externa — ninguna de las dos cifras cuadra con `git diff --shortstat origin/develop...HEAD` ni con el conteo de GitHub. Con el bloqueante cerrado el diff queda en **+162 / −14** (+141 / −14 sin bitácora), 9 archivos.
- **[docs]** El comentario de `getTodayAsMaxIssueDate` en el modal decía la regla pero no dónde se hace cumplir; ahora aclara que el tope del calendario es sólo UX y que quien la aplica es el test del schema. Sin este puntero, quitar una de las dos mitades parece inocuo.
- **Autorrevisión (`pr-readiness`, sin agente independiente):**
    - **Rutas de escritura (defecto recurrente #2):** `DeferredPaymentDocumentSchema` tiene un solo consumidor (`useDeferredPaymentForm:149`) y `createDeferredPayment`/`updateDeferredPayment` un solo despachador (mismo hook). No hay autosave ni segunda ruta que esquive Yup, a diferencia de revisiones técnicas.
    - **Contrato remoto verificado en la implementación real:** `StoreDeferredPaymentDocumentRequest:50` exige `required|date_format:Y-m-d` y `UpdateDeferredPaymentDocumentRequest:56` `sometimes|date_format:Y-m-d`; **ninguna regla `before_or_equal:today`**. La validación nueva es sólo de cliente: la API sigue aceptando una emisión futura. Queda declarado como pendiente en el cuerpo del PR — el backend es de sólo lectura desde este repo.
    - **Rama atrasada respecto de `develop` — resuelto:** la merge-base era `f549fa77` (#188) y `develop` ya había avanzado a `e85e95c7` (#191) y `f1920084` (#192), con #191 refactorizando **este mismo modal** al `CustomerSaleSelect` compartido. Se trajo `develop` a la rama con el merge `c35660ab`, **sin conflictos**, y toda la verificación se rehízo sobre el resultado.
    - **Línea base de lint corregida:** comparar contra el tip de `develop` daba +3 falsos en el modal, que en realidad eran de #191 (arregló allí esos `import/order`); tras el merge desaparecen solos. Además, `import/order` no es determinista acá: el mismo archivo da 1 hallazgo por lote y 0 individual, por el resolver TypeScript roto que ya arrastra el repo — por eso la comparación usa el mismo comando y el mismo conjunto en ambos lados.
    - **Deuda de CI reconfirmada:** `bitacora_trabajo/04-09-2026.md` sigue fallando Prettier en `develop` y #190 sigue abierto, así que la explicación del cuerpo del PR continúa vigente.
- **[chore]** Merge `c35660ab` de `origin/develop` (#191, #192) en la rama, sin conflictos — ni de código ni de la bitácora del día. El modal conserva las dos cosas: el `maxIssueDate`/`maxIssueYear` del PR y el `CustomerSaleSelect` de #191.
- **Verificación (3.ª pasada, sobre el merge):** `typecheck` 0 errores; `test:run` **120 suites / 846 pruebas** en verde (subió de 117/822 porque #191 y #192 sumaron suites); `test:related` sobre los 4 archivos de producción 12 suites / 138; `eslint` sobre los 8 archivos de código del diff **17 hallazgos, exactamente los mismos 17 de `origin/develop`** con el mismo comando y conjunto → 0 nuevos; `check:eol` y `lint:imports` (gates de CI) en verde; `prettier --check` y `git diff --check` limpios.
- **Defecto recurrente #4 revalidado sobre el merge:** las cuatro mutaciones vuelven a matar su prueba — anular la regla del schema, cambiar `<=` por `<`, revertir el hook a `develop` (`expected '2026-09-07' to be ''`) y revertir la tabla a `develop` (`Unable to find an element with the text: 07-07-2026`). Todas revertidas.
- **[fix]** CI destrabado: `bitacora_trabajo/04-09-2026.md` llegaba desde `develop` sin la línea en blanco ni el separador `---` antes del encabezado `## Tema: ZF-99`, y hacía fallar `prettier:check` en todo PR abierto contra `develop`. Se aplica la corrección **idéntica byte a byte** a la que ya tiene #190 (`79ba244e`), de modo que si ambas ramas llegan a `develop` el cambio converge sin conflicto. Es sólo formato: no se toca una palabra del contenido. Con eso los **cinco gates** de `Calidad de PR` pasan en local — `check:eol`, `prettier:check` (repo completo, 0 archivos con problemas), `lint:imports`, `typecheck` y `test:run` (120 suites / 846 pruebas).
- **Estado:** Implementación de la card ZF-105 completa (formulario + tabla), bloqueante de validación cerrado y todo reverificado sobre `develop` al día. Commit, push y actualización del PR #193 pendientes de autorización.
