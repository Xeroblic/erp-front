# Bitácora — 20-08-2026

## Temas del día

- PR #151 — eliminación segura de perfiles de crédito
- ZF-41 — exportación Excel de cartera y pagos diferidos
- ZF-38 — correcciones de revisión del PR 153
- ZF-37 — foco del indicador de desplazamiento
- ZF-43 y ZF-32 — agenda de recordatorios y texto de eliminación

---

## Tema: PR #151 — eliminación segura de perfiles de crédito

**Rama:** `fix/zf-33-eliminar-perfil-credito`

- **[fix]** La suspensión previa al DELETE conserva los valores del perfil cargado y no persiste el borrador de condiciones sin confirmación.
- **[fix]** Cancelar la confirmación iniciada desde la papelera de la tabla cierra el modal en lugar de revelar el editor.
- **[test]** Se añadieron regresiones para el DELETE 422 con borrador modificado y para la cancelación de apertura directa.
- **[fix]** El fallo del DELETE posterior a una suspensión confirmada informa que la suspensión quedó aplicada; la confirmación advierte el descarte del borrador y muestra los bloqueos por saldo pendiente.
- **[test]** Se cubrieron el fallback del DELETE sin mensaje y el bloqueo visible en la apertura directa desde la cartera.
- **PR / commit:** PR #151 actualizado con las correcciones finales.

---

## Tema: ZF-38 — correcciones de revisión del PR 153

**Rama:** `feat/zf-38-calculo-bruto-desde-neto`

- **[fix]** Se incorporó el desglose referencial de IVA al drawer de detalle, se restituyó el rótulo dinámico del precio unitario y se eliminó la etiqueta duplicada del checkbox.
- **[fix]** Se ajustó la grilla del precio y el checkbox: los rótulos compactos se muestran en una línea y el control queda alineado con el input en desktop.
- **[test]** Se cubrieron el desglose de total entero y decimal, la etiqueta de edición y el nombre accesible del control.
- **[fix]** Se corrigió el punto tecleado como separador decimal: al escribir sobre un valor ya formateado (`$ 50.411` + `.`), el punto final se interpreta como decimal en curso y deja de leerse como separador de miles, que multiplicaba el monto por 100.
- **[refactor]** Se reemplazó el `md:pt-9` del checkbox de IVA por `reservesLabelSpace` en `DeferredPaymentField`: la altura del rótulo queda definida en un único lugar y la alineación deja de depender de una medida replicada en el modal.
- **PR / commit:** PR #153 actualizado con las correcciones de revisión.
- **Estado:** correcciones verificadas y enviadas a la rama del PR.

## Tema: ZF-37 — foco del indicador de desplazamiento

**Rama:** `fix/zf-37-atenuar-fondo-modales-pagos-diferidos`

- **[fix]** Se incorporó el indicador fijo de desplazamiento a la frontera de foco de `Modal` para que Tab y Shift+Tab no alcancen controles del fondo.
- **[fix]** El indicador quedó dentro de `Dialog` con eventos habilitados, para que su activación no se interprete como cierre por backdrop.
- **[test]** Se agregó la regresión que recorre el indicador y verifica ambos límites del trap.
- **[test]** La regresión verifica que mouse y touch sobre el indicador no solicitan cerrar el modal.
- **[fix]** El trap de foco recupera el control cuando `activeElement` queda fuera de la capa (pasa a `body` al desmontarse el control enfocado, por ejemplo al quitar un ítem); antes Tab escapaba al fondo.
- **[fix]** El nivel de la pila se deriva de los modales vivos en vez de un contador que sólo crecía: reabrir un modal sobre otro subía el z-index sin cota (1085 → 1105 → 1125 → …) hasta cruzar capas como `AttachmentsDropOverlay` (1100).
- **[refactor]** Se tiparon los handlers de backdrop como `MouseEvent | TouchEvent`, eliminando cuatro `@ts-ignore` y los `any` que arrastraban.
- **Referencia:** segunda revisión del PR #152. Estado: correcciones aplicadas y verificadas (`tsc` 0 errores, ESLint limpio en los archivos del PR, suites de `ui` y de Pagos Diferidos en verde salvo los 3 fallos de fechas preexistentes en `develop`).

---

## Tema: ZF-43 y ZF-32 — agenda de recordatorios y texto de eliminación

**Rama:** `fix/zf-43-32-recordatorios-texto`

- **[fix]** Se actualizó la explicación de recordatorios para mostrar la agenda interna derivada de la cadencia del cliente, con el ejemplo vigente desde D-7 hasta D+20.
- **[fix]** Se reemplazó el voseo «Anulá primero» por «Anula primero» en la advertencia de documentos con abonos.
- **[test]** Se ajustaron las pruebas del modal de recordatorios y del drawer de eliminación para respaldar el nuevo contenido.
- **[fix]** Se mantuvo visible el horario diario de evaluación de los recordatorios (08:30, hora de Santiago).
- **Estado:** implementación local validada; sin commit, push ni PR.

---

## Tema: ZF-41 — exportación Excel de cartera y pagos diferidos

**Rama:** `feat/exportar-excel-cartera-y-pagos-diferidos`

- **[feat]** Se integraron exportaciones `.xlsx` de cartera de crédito y pagos diferidos con filtros efectivos, página actual o todo lo filtrado.
- **[fix]** Se reinicia el estado de exportación al cambiar de subsidiaria, se impide reutilizar respuestas obsoletas y se centraliza la omisión de paginación en la opción de exportar todo.
- **[fix]** Las dos vistas bloquean y explican la exportación mientras exista un orden local que el backend no puede reproducir.
- **[fix]** El orden local se reinicia al cambiar de subsidiaria para no heredar un bloqueo de exportación de otra filial.
- **[test]** Se agregaron pruebas de servicio, exportación, permiso/contexto, parámetros de paginación y retorno de subsidiaria.
- **[refactor]** `exportFiltersForRequest` volvió a derivarse por desestructuración del filtro del listado, para que un filtro nuevo llegue al archivo exportado sin edición manual.
- **[style]** Se reemplazó el voseo restante de la interfaz («Seleccioná», «Restablecé») por español neutro en cartera de crédito, pagos diferidos y clientes de ventas.
- **Estado:** correcciones de revisión verificadas y listas para publicar.
