# Bitácora — 29-07-2026

## Temas del día

- Integración API de pagos diferidos

---

## Tema: Integración API de pagos diferidos

**Rama:** `feat/integracion-api-pagos-diferidos` · **Base:** `feat/pagos-diferidos`

- **[feat]** Se definieron contratos HTTP tipados para documentos, abonos y perfiles de crédito.
- **[feat]** Se creó un servicio de dominio para todos los endpoints documentados bajo `subsidiaries/{subsidiary}`.
- **[refactor]** La capa API normaliza recursos Laravel directos o envueltos en `data`, propaga `AbortSignal` e invalida caché después de mutaciones.
- **[test]** Se verificaron rutas, parámetros, envelopes, mutaciones, invalidación de caché, abonos y perfil de crédito.
- **[feat]** Los thunks de listado y detalle consumen el servicio tipado y sólo envían los filtros admitidos por el backend.
- **[test]** Se verificó la propagación de AbortSignal y que una cancelación no deje errores falsos en Redux.
- **[feat]** El resumen consume el endpoint real y la UI usa el bucket `current` definido por el backend.
- **[fix]** Las fechas `dd/mm/aaaa` del backend se normalizan en el servicio a ISO interno para evitar errores de render; los filtros se convierten de vuelta al formato de la API.
- **[config]** El mock quedó desactivado en desarrollo para validar los datos reales del backend.
- **Decisión:** El backend es la fuente de verdad para estados, vencimiento, montos y estructura. La capa de servicio sólo adapta formatos técnicos para el consumo seguro de la UI.

### Validación

- TypeScript: `tsc --noEmit` sin errores.
- Pruebas focales: 3 archivos y 19 pruebas aprobadas.
- Suite completa: 41 archivos y 221 pruebas aprobadas.
- ESLint focalizado: sin errores propios; permanece `import/extensions` preexistente.
- `git diff --check` sin errores.

**Estado:** contratos, servicio, listado, detalle y resumen conectados; maqueta adaptada al contrato real del backend.