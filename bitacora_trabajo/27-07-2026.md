# Bitácora — 27-07-2026

## Temas del día

- Inicio de ZF-6: detalle de pagos diferidos
- Segunda revisión del dashboard de pagos diferidos (ZF-5)
- Tercera revisión del dashboard de pagos diferidos (ZF-5)
- Cuarta revisión del dashboard de pagos diferidos (ZF-5)
- Quinta revisión del dashboard de pagos diferidos (ZF-5)

---

## Tema: Segunda revisión ZF-5

**Rama:** `feat/dashboard-pagos-diferidos` · **Issue:** ZF-5 · **PR base:** `feat/pagos-diferidos`

- **[fix]** El mock quedó desactivado en producción y ejemplo, ausente del entorno común y activo solamente en desarrollo.
- **[refactor]** El flag mock se aisló en un módulo configurable para que las pruebas no dependan de archivos `.env`.
- **[fix]** La tabla ya no muestra “0 documentos” ni un estado vacío cuando la consulta falla.
- **[fix]** La paginación conserva sus metadatos durante recargas del mismo contexto y se limpia al cambiar de subsidiaria o fallar.
- **[fix]** Ambos filtros de vencimiento permiten fechas futuras hasta diez años y los rangos inválidos no disparan consultas.
- **[fix]** La tabla se oculta mientras el rango es inválido para no mostrar resultados obtenidos con filtros anteriores.
- **[fix]** Los KPI toleran grupos ausentes del resumen sin provocar una pantalla en blanco.
- **[fix]** `hasFilters` usa la búsqueda debounced, en sincronía con la consulta visible.
- **[refactor]** Se retiraron manejos de promesas sin efecto y `dedupe` de solicitudes cancelables para evitar heredar abortos.
- **[fix]** La configuración de página usa `ERP_PERMISSIONS.DEFERRED_PAYMENTS.VIEW` y declara `requireAll: false`.
- **[refactor]** Se retiraron reformateos y cambios ajenos en `pages.config.ts` y `format.utils.ts`.
- **[test]** Se aisló el flag mock dentro de las pruebas y se agregó cobertura para paginación estable, error sin estado vacío, rango inválido y Tooltip activado por foco.

### Validación

- `loadEnv('production')`: `VITE_DEFERRED_PAYMENTS_MOCK=false`.
- TypeScript: `tsc --noEmit` sin errores.
- Pruebas focalizadas: 4 archivos y 16 pruebas aprobadas.
- Suite global: 36 archivos y 184 pruebas aprobadas.
- ESLint focalizado del módulo: sin errores propios, descontando `import/extensions` preexistente.

### Pendientes del backend

- Publicar nombres definitivos de permisos.
- Aceptar `overdue` separado de `status` en el contrato del listado.
- Formalizar `days_until_due` como nullable.
- Validar el rango de fechas en el FormRequest.
- Garantizar `personalizacion.subsidiary_id` en `/me`.

**Estado:** cuatro bloqueantes de la segunda pasada corregidos y verificados; ZF-5 permanece abierto.

## Tema: Tercera revisión ZF-5

**Rama:** `feat/dashboard-pagos-diferidos` · **Issue:** ZF-5 · **PR base:** `feat/pagos-diferidos`

- **[fix]** El error y su botón de reintento se ocultan mientras el rango de fechas es inválido, evitando controles inertes y mensajes contradictorios.
- **[fix]** La tabla mantiene una fila explicativa dentro del cuerpo cuando falla la consulta.
- **[fix]** La búsqueda desde páginas posteriores espera el debounce y emite una sola consulta con página 1.
- **[refactor]** La validación del rango se calcula una sola vez en el hook y se entrega a los filtros.
- **[fix]** El resumen se conserva durante recargas del mismo contexto y se limpia al cambiar de contexto o fallar.
- **[refactor]** El hook dejó de seleccionar el slice completo y se retiró su memoización decorativa; KPI y tabla se memoizaron con callbacks estables.
- **[fix]** Tooltip compone los handlers del hijo y su trigger fallback es alcanzable por teclado.
- **[chore]** La variable `VITE_DEFERRED_PAYMENTS_MOCK` quedó en la declaración Vite canónica y se eliminó el archivo duplicado.
- **[chore]** Se retiró la coma de formato ajena en `pages.config.ts`.
- **[test]** Se cubrieron búsqueda paginada sin consulta redundante, persistencia del resumen, fila de error y accesibilidad de Tooltip.

### Validación

- TypeScript: `tsc --noEmit` sin errores.
- Pruebas focalizadas: 4 archivos y 20 pruebas aprobadas.
- Suite global: 36 archivos y 188 pruebas aprobadas.
- ESLint: sin errores propios; permanece únicamente `import/extensions` preexistente al revisar el diff completo.

## **Estado:** bloqueante y deuda de la tercera pasada corregidos; ZF-5 permanece abierto.

## Tema: Cuarta revisión ZF-5

**Rama:** `feat/dashboard-pagos-diferidos` · **Issue:** ZF-5 · **PR base:** `feat/pagos-diferidos`

- **[fix]** El cambio de subsidiaria reinicia la página en el store y la primera consulta del nuevo contexto sale directamente con `page: 1`.
- **[fix]** El error de lista y su reintento se ocultan durante el debounce de búsqueda.
- **[fix]** La tabla no renderiza filas de datos cuando existe un error y usa una referencia estable para las columnas de paginación.
- **[fix]** El Tooltip fallback tiene nombre accesible y permite abrir o cerrar la ayuda mediante clic.
- **[fix]** `ProtectedButton` implementa realmente el fallback deshabilitado usado por el placeholder de ZF-7.
- **[refactor]** Los consumidores de ventas y marcas importan `formatCLP` desde la utilidad compartida y se eliminó el re-export intermedio.
- **[refactor]** Se retiró la rama de ordenamiento inalcanzable del mock y se corrigió el contrato de la variable Vite.
- **[chore]** Se revirtió la corrección de tilde ajena en el footer compartido y se evitó incluir reformateos amplios de Prettier.
- **[test]** Se cubrieron el cambio de subsidiaria, el reinicio de página, filas bajo error, Tooltip fallback y `ProtectedButton` deshabilitado.

### Validación

- Pruebas focalizadas: 5 archivos y 22 pruebas aprobadas.
- TypeScript: sc --noEmit sin errores.
- Suite global: 37 archivos y 190 pruebas aprobadas.
- ESLint focalizado: sin errores; solo warnings preexistentes de prop spreading en ProtectedButton.

**Estado:** bloqueante y deuda de la cuarta pasada corregidos; ZF-5 permanece abierto.

## Tema: Quinta revisión ZF-5

**Rama:** `feat/dashboard-pagos-diferidos` · **Issue:** ZF-5 · **PR base:** `feat/pagos-diferidos`

- **[fix]** Se retiró de ZF-5 el botón provisional “Nuevo documento”; la acción funcional se incorporará directamente en ZF-7.
- **[refactor]** `ProtectedButton` volvió al comportamiento de la rama base y se eliminó la prueba que sustituía sus componentes reales.
- **[fix]** El fallback de Tooltip volvió a ser un `span` descrito mediante `aria-describedby`, y el popper expone `role='tooltip'` sin insertar controles interactivos dentro de `label`.
- **[refactor]** La paginación usa un controlador mínimo y dejó de construir un row model de React Table que el pie no consume.
- **[fix]** Lista y resumen esperan el mismo debounce para no mezclar KPI y documentos durante un cambio de subsidiaria.
- **[fix]** Los documentos pagados muestran “—” con etiqueta “No aplica” en la situación de vencimiento.
- **[docs]** El filtro `overdue` deja registrado junto a su opción el contrato pendiente de backend para separarlo de `status`.
- **[fix]** Las filas declaran su acción interactiva y conservan activación mediante clic, Enter y espacio.
- **[chore]** ESLint quedó alineado con el runtime JSX configurado para permitir retirar imports React sin uso.
- **[test]** Se reemplazaron pruebas artificiales por cobertura del Tooltip real, teclado de filas, vencimiento no aplicable y sincronización durante debounce.

### Validación

- TypeScript: `tsc --noEmit` sin errores.
- Pruebas focalizadas: 3 archivos y 14 pruebas aprobadas.
- Suite global: 36 suites y 192 pruebas aprobadas.
- ESLint focalizado: sin errores ni warnings propios, descontando `import/extensions` al revisar el diff completo.

**Estado:** bloqueante y deuda de la quinta pasada corregidos; ZF-5 permanece abierto.

## Tema: Inicio ZF-6 — contrato y carga del detalle

**Rama:** `feat/detalle-pagos-diferidos` · **Issue:** ZF-6 · **PR base:** `feat/pagos-diferidos`

- **[feat]** Se definieron contratos estrictos para encargados, ítems, abonos, adjuntos y documento completo.
- **[feat]** El mock permite obtener un detalle estable por ID y responde con error ante documentos inexistentes.
- **[feat]** Redux incorpora `current`, carga y error independientes para el detalle, cancelación y protección por `requestId`.
- **[test]** Se cubrieron coherencia del mock, carga vigente, respuesta obsoleta, error, limpieza y aborto.
- **[fix]** El contrato permite omitir vatar_url y usa el nombre de dominio IDeferredPaymentAbono.
- **[fix]** El mock incluye un documento con múltiples abonos cuya suma coincide con paid_amount.
- **[fix]** La recarga del mismo documento conserva el detalle visible y solo limpia al cambiar de documento o contexto.
- **[test]** La cancelación del mock se verifica con un AbortSignal real.

### Validación

- TypeScript: `tsc --noEmit` sin errores.
- Pruebas focalizadas: 2 suites y 17 pruebas aprobadas.
- Suite global: 37 suites y 199 pruebas aprobadas.
- ESLint focalizado: sin errores propios.

**Estado:** primer paso de ZF-6 completado; la interfaz del drawer queda para el siguiente bloque.
