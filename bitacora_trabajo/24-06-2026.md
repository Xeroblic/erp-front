# Bitácora — 24-06-2026

> Horas tomadas del `git reflog` (hora local real de cada commit/checkout/pull/merge).
> El trabajo del hub de integraciones (PRs #35/#36) arrancó la **noche del 23** y cruzó
> medianoche; queda registrado en `23-06-2026.md`. Aquí va la sesión nocturna del 24.

## Sesión 22:10 – 23:00 · rama `fix/jwt-refresh-session-expiry-1h`

- **[fix]** La sesión de usuario se cerraba tras un rato de uso. Síntoma: `POST /api/refresh`
  daba **500** y luego todo caía en **401** → login. _(commit `7e805e3`)_
- **Causa raíz (backend, solo lectura):** `bootstrap/app.php` no tiene handler para
  `TokenExpiredException` / `JWTException`, así que `/refresh` las devuelve como 500 en vez
  de 401. El front interpretaba ese fallo como sesión perdida y hacía `logout()`.
- **[fix]** Mitigación front: el `logout()` ante fallo del refresh en `BaseService.ts` ahora
  es **condicional** (solo 401/403 o token fuera de la ventana de refresh; un 500/red ya no
  cierra sesión).
- **[fix]** Se subió `VITE_JWT_REFRESH_MARGIN_SECONDS` 15 → 300 (refresca el token antes de
  expirar) en `.env`, `.env.production` _(commit `7e805e3`)_ y `.env.development`
  _(commit `c337850`)_.
- **[refactor]** Limpieza de `BaseService.ts` (≈398 → 195 líneas): se quitó código legacy
  comentado, comentarios duplicados y un campo sin uso; respuesta del refresh tipada.
- **[chore]** `.env.development`: API base URL apuntando al puerto local **8001**
  _(commit `4cbdc3e`)_.
- **[docs]** Se creó `bitacora_trabajo/` con `instrucciones.md` (reglas del registro diario)
  y la sección 13 "Bitácora de trabajo" en `CLAUDE.md` _(commit `7d7d224`)_.
- **Config (referencia):** backend TTL = 30 min en prod / 1440 en local; `refresh_ttl` =
  7 días (el front ya usa el mismo valor).
- **Pendiente backend (solo lectura, NO tocado):** agregar handlers `render` para
  `TokenExpiredException` / `JWTException` → 401; evaluar `leeway` > 0.
- **PR:** #41 → mergeado a `develop` ≈ 23:00 (label `bug`, reviewer `Xeroblic`).
- **Estado:** terminado.

## Sesión 23:23 – 23:37 · rama `feat/login-recordar-cuenta`

- **[feat]** `src/utils/rememberedAccount.ts`: helper de "Recordar cuenta" que persiste
  **solo el email** (nunca la contraseña) en `localStorage`. _(commit `3a6301c`)_
- **[fix]** `src/utils/appStorage.ts`: `clearAppStorage` preserva la cuenta recordada para
  que sobreviva al logout. _(commit `55bdd6f`)_
- **[feat]** `src/pages/Login.page.tsx`: checkbox "Recordar cuenta" funcional (pre-rellena
  el email, controlado por Formik, guarda/olvida tras login exitoso). _(commit `e50a869`)_
- **Motivo:** el checkbox existía en el JSX pero no hacía nada (sin estado ni handler).
- **Decisión:** commits granulares (uno por archivo/función).
- **PR:** #42 → mergeado a `develop` ≈ 23:37.
- **Estado:** terminado.
