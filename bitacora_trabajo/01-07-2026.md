# Bitácora — 01-07-2026

## Sesión · rama `fix/recalcular-grado-sugerido` (desde `develop`)

Fix del botón **"Recalcular"** en el Paso 3 (Calificación) de la revisión técnica: al
modificar una revisión y recalcular, mostraba el grado **actual** del equipo en vez del
**nuevo grado sugerido** por el backend.

- **Causa:** el backend, al recalcular (`reopen-review` + `complete-review`), devuelve
  `grade` (grado vigente del equipo, no cambia) y `suggested_grade` (la nueva sugerencia).
  El front tomaba el campo equivocado:
  1. `handleRecalculateGrade` (`useItemReview.ts`) hacía
     `suggested_grade: grading?.grade || grading?.suggested_grade` → tomaba `grade` primero.
  2. `index.tsx` muestra `item.approved_at ? item.grade : item.suggested_grade`, y recalcular
     no limpiaba `approved_at`, así que seguía mostrando `item.grade`.
- **[fix]** En `handleRecalculateGrade`: priorizar `suggested_grade || grade`, limpiar
  `approved_at: null` (recalcular reabre la revisión → deja de estar aprobada, así el display
  usa la nueva sugerencia) y alinear `automaticGrade` a `suggested_grade`. `approved_at` sólo
  se usa en ese display, sin efectos colaterales (`isApproved` deriva de `review_status`).
- **[chore]** Incluye un cambio previo del usuario: `IngresoStockView` con `PageWrapper`
  (`title='Ajuste Stock'`, `isProtectedRoute`), commit aparte.
- **Validación:** `tsc --noEmit` sin errores.

**Estado:** terminado a nivel de código, en `fix/recalcular-grado-sugerido`.

## Sesión 2 · rama `feat/woo-toggle-off-warning` (desde `develop`)

El toggle "Sincronizar con WooCommerce" al apagarse despublicaba directo (solo toast). Se
agrega una advertencia fuerte porque despublicar = enviar el producto a la papelera de Woo.

- **[feat]** `WooCommercePublishPanel`: al **apagar** el toggle ya no despublica de una;
  abre un modal de confirmación (rojo, ícono de papelera) con el mensaje claro *"Esta acción
  eliminará el producto publicado en WooCommerce"* → se envía a la **papelera** de la tienda.
- **[feat]** El modal indica la alternativa: para **desvincular sin borrar** usar el botón
  *"Desvincular de WooCommerce"* de la sección **Emparejamiento manual** (mantiene el producto
  vivo en Woo y solo corta la sincronización). Botón de confirmación: "Sí, eliminar de
  WooCommerce" con estado de carga.
- **Encender** el toggle (publicar) sigue siendo directo, sin fricción.
- **Validación:** `tsc --noEmit` sin errores.

**Estado:** terminado a nivel de código, en `feat/woo-toggle-off-warning`.

## Sesión 3 · rama `feat/woo-sync-guide` (sobre `feat/woo-toggle-off-warning`)

Guía visual grande que explica el panel de WooCommerce la primera vez que se abre, ya que
la sincronización tiene reglas por campo (unidireccional vs bidireccional) no evidentes.

- **[feat]** Nuevo `WooSyncGuide.tsx`: modal explicativo con desglose por campo y badges de
  dirección (ERP→Woo forzado para stock; ERP↔Woo bidireccional para precio, descripción
  corta/larga, categorías y marcas). Aclara que el precio sincroniza desde el precio de
  canal (no el precio base del ERP) y que nombre/precio son propios por canal (agnóstico al
  marketplace). Advertencia de que el stock lo controla siempre el ERP.
- **[feat]** `WooCommercePublishPanel`: la guía se muestra automáticamente la primera vez
  (flag `woo_sync_guide_seen_v1` en localStorage) y queda disponible con un botón
  "Cómo funciona" en el header.
- **Validación:** `tsc --noEmit` sin errores.

**Estado:** terminado a nivel de código, en `feat/woo-sync-guide` (stack sobre #55).

- **[feat]** Diagnóstico Woo: la columna Estado del ERP ya no muestra `—`; ahora refleja
  "Publicado"/"No publicado" según `isPublishedHere` y marca "Difiere" (rojo) si no concuerda
  con el estado real de Woo (`publish`). Antes el `—` confundía (parecía dato faltante).
