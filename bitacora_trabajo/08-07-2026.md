# Bitácora — 08-07-2026

## Temas del día

- Análisis subsidiaria y cambio de contexto (hooks)
- Refactor org-context — core y contratos /perfil
- Contrato crudo IPerfilUser / IPerfilResponse
- useOrgContextSwitcher — centralizar switch
- Limpieza useCompanyManager + inventory sin any
- Eliminar useUserBranches deprecated
- Consolidar switch + evento org-context-changed
- Fix id de IUserMe (pk → id)

---

## Tema: Análisis subsidiaria y cambio de contexto (hooks)

**Rama:** `develop` / base del refactor · **Tipo:** auditoría

Auditoría de subsidiaria y cambio de contexto (company/subsidiary/branch) en hooks y
piezas mínimas (selectores, personalización, header).

**TL;DR canónico:** `useCurrentBranch` + `selectEffectiveSubsidiaryId` + `useAuthorization`.

**Problemas hallados:**

- Dos caminos de switch (sucursal vs company) con evento `user-branch-changed` semánticamente roto (subsidiary_id dentro de `branchId` en camino B).
- `useCompanyManager`: `company_id: 1` hardcodeado, `any`, logs, `canAccess*` pobre.
- Código muerto: `useContextChangeNotifier` (render loop), `useUserSubsidiaries`.
- `useUserBranches` deprecated pero ~11 usos.
- Doc engañoso `PREFERRED_SUBSIDIARY_USAGE.md` (feature nunca construido).

**Recomendaciones P0–P2** ejecutadas en los temas siguientes del día y en 09-07.

---

## Tema: Refactor org-context — core y contratos /perfil

**Rama:** `refactor/org-context-hooks`

- **[refactor]** Core puro `src/utils/orgContext.util.ts` (`toContextId`,
  `resolveSubsidiaryFromBranch`, `BranchLike`/`ContextUser`). Elimina lógica duplicada entre
  `useCurrentBranch` y `subsidiarySelectors`.
- **[refactor]** Ambos consumen el core; API pública intacta (~63 consumidores).
- **[chore]** Borrados `useContextChangeNotifier.ts` y `useUserSubsidiaries.ts` (0 usos).
- **[docs]** Eliminado `PREFERRED_SUBSIDIARY_USAGE.md`.
- **[test]** `orgContext.util.test.ts` (vitest con deuda de entorno; `tsc --noEmit` OK).
- **[refactor]** Tipos alineados al contrato real de `/perfil` (id = `id`, subsidiary objeto o
  `subsidiary_id`); sin claves inventadas.
- **[fix]** `IBranch.primary_warehouse_id`; `IPersonalizacionUsuario` solo campos reales
  requeridos; dedup `ICommuneRef`/`IManagerRef`/`INormalizedEntityAliases`; `IUser` sin `any`
  en company/subsidiary/branch.
- **[fix]** `IUserMe`: `gender` → `genero`; campos faltantes reales; caveat de `id` anotado.

**Estado:** typecheck/lint OK; pasos commiteados.

---

## Tema: Contrato crudo IPerfilUser / IPerfilResponse

**Rama:** `refactor/org-context-hooks` (pusheada)

- Hallazgo: `UserWithDetails extends IUserMe` → sesión y admin comparten base; no borrar
  alias que usa admin sin app corriendo.
- **[refactor]** `IPerfilUser` + `IPerfilResponse`; `authSlice` deja de tipar crudo con `IUserMe`.
- **[docs]** JSDoc en `IUserMe` (doble rol + caveat `id`).

**Pendiente (otros temas del día / follow-ups):** migrar `useUserBranches`, centralizar
switcher, `canAccess*` → `useAuthorization`.

---

## Tema: useOrgContextSwitcher — centralizar switch

**Rama:** `refactor/org-context-switcher` (base `origin/develop`, indep. PR #73)

- **[refactor]** `useOrgContextSwitcher`: `switchContext({ sucursalPrincipal, subsidiaryId?,
companyId?, eventBranchId? })`, `isSwitching`.
- **[fix]** Quita `company_id: 1`; resuelve company real; emite `org-context-changed` tipado
    - legacy `user-branch-changed` con semántica corregida (subsidiary ya no va en `branchId`).
- **[fix]** `useCompanyManager.switchCompany` delega en el switcher.

**Pendiente verificar en app:** CompanySelectorButton + `useIngresoStock`.
**No migrado aún:** `SelectSucursalEmpresa` (usaba thunk de personalización; ver tema consolidar).

---

## Tema: Limpieza useCompanyManager + inventory sin any

**Rama:** `refactor/context-cleanup` (sobre switcher / PR #74)

- **[refactor]** #6 — `useCompanyManager` sin `any` ni `console`; helper
  `collectAccessibleSubsidiaryIds`; lint 32 → 7 (`import/extensions` repo-wide).
- **[refactor]** #7 — `canAccess*` delega en `useAuthorization`.
- **[refactor]** #12 — `inventory.interface` performer/creator/releaser → `IUser`.

**Estado:** `tsc` OK, sin cambio de comportamiento intencional.

---

## Tema: Eliminar useUserBranches deprecated

**Rama:** `refactor/drop-useuserbranches`

- **[refactor]** Selector `selectUserBranches` + `UserBranchInfo` desde store (sin API).
- **[chore]** Borrados hook deprecated, alias, re-export y barrel huérfano.
- **[refactor]** Migrados: `SelectSucursalEmpresa`, `DetalleProveedor`,
  `FinalizeAdjustmentModal`, `useIngresoStock`, `useTrazabilidadMovimientos`,
  `TrazabilidadList`, `SucursalConfigForm`, `UserBranchSelector`.

**Nota UI:** `UserBranchSelector` sin spinner (dato síncrono del store).

---

## Tema: Consolidar switch + evento org-context-changed

**Rama:** `refactor/consolidate-switch-events` (develop con #73–76)

- **[refactor]** #3 — `SelectSucursalEmpresa` delega en `useOrgContextSwitcher`.
- **[refactor]** Switcher usa `actualizarSucursalPrincipalThunk` + `eventSubsidiaryId`.
- **[refactor]** #4 — eliminado legacy `user-branch-changed`; solo `org-context-changed`
  (`App.tsx`, `useIngresoStock`).

**Verificar en app:** cambio sucursal/empresa; nota: SelectSucursal ahora también dispara
`userMeThunk`.

---

## Tema: Fix id de IUserMe (pk → id)

**Rama:** `refactor/fix-userme-id`

- **[fix]** #10 — `normalizeUserProfile` mapea `pk → id`; el tipo deja de mentir.
- **[docs]** Caveat JSDoc de `id` actualizado.

**Pendiente #9/#11:** partir sesión vs admin (hecho en gran parte el 09-07).
