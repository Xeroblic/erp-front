# 08-07-2026 — Limpieza de contexto (useCompanyManager + tipos)

## Sesión — Limpiezas sin app (#6, #7, #12)

**Rama:** `refactor/context-cleanup` (apilada sobre `refactor/org-context-switcher` / PR #74,
porque #6/#7 tocan `useCompanyManager`, ya modificado ahí).

- `[refactor]` **#6 — `useCompanyManager` sin `any` ni `console`**: extraído helper
  `collectAccessibleSubsidiaryIds(user)` (DRY, elimina los `(user as any).companies.forEach`
  duplicados); tipado `UserCompanyLike` para los items de `user.companies`;
  `catch (error: unknown)` con narrowing; eliminados todos los `console.log`/`warn`/`error`
  de debug. Lint: **32 → 7 errores** (los 7 restantes son `import/extensions` repo-wide).
- `[refactor]` **#7 — `canAccess*` delega en `useAuthorization`**: se eliminan las tres
  implementaciones locales (lógica más pobre, solo comparaban `user.subsidiary?.id`) y se
  reexportan las de `useAuthorization` (usa los sets `access`/`visible`). Sin consumidores
  externos afectados (nadie las usaba fuera del hook).
- `[refactor]` **#12 — `inventory.interface.ts` sin `any`**: `performer/creator/releaser`
  pasan de `any // IUser` a `IUser`. Seguro: los consumidores sólo leen `.performer?.name`.
  Lint inventory: 23 → 20.

**Estado:** `tsc --noEmit` OK. Sin cambios de comportamiento.

**No hecho (a propósito):**
- **#13** (`ISucursal`/`ISubempresa` normalizar una vez en el servicio): no es limpieza
  rápida, es reestructurar los normalizadores (`sucursalesSlice`/`empresaSlice`) + quitar
  el alias soup + migrar consumidores → conviene con la app corriendo.
- **#8** (migrar ~11 consumidores de `useUserBranches` deprecated): 11 archivos, verificación
  parcial con app.
- **#9-#11** (partir `IUserMe` sesión vs admin, arreglar `id`): refactor grande con app.
