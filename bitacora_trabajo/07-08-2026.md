# Bitácora — 07-08-2026

## Temas del día

- Acceso a «Gestión de usuarios» para el rol Cobranza (ZF-15)
- Cartera de crédito ZF-16 — acceso y edición
- Detalle de cliente — jerarquía visual
- Pagos diferidos ZF-7 — bloqueo duro de cupo
- ZF-19 — título seguro del detalle de cliente
- ZF-14 — Vista de activación de usuario falla por hook condicional

## Tema: Pagos diferidos ZF-7 — bloqueo duro de cupo

**Rama:** `fix/bloqueo-cupo-credito-documentos`

- **[fix]** ZF-7 trata los rechazos 422 de perfil de crédito inactivo y cupo superado como errores autoritativos del backend, conservando el borrador y sin confirmar la mutación.
- **[refactor]** Se retiró el estado interno que interpretaba `credit_limit_exceeded` como un guardado exitoso con advertencia; el campo de la respuesta se mantiene por compatibilidad del contrato.
- **[test]** Se cubrieron ambos mensajes planos de ZB-74 para alta y aumento de total en edición.
- **Estado:** Terminado; pruebas focalizadas, TypeScript y revisión de diff aprobados.

---

## Tema: Acceso a «Gestión de usuarios» para el rol Cobranza (ZF-15)

**Rama:** `feat/cobranza-rol-pagos-diferidos-zf15`

- **[fix]** El ítem «Gestión de usuarios» y sus rutas (`/gestion/roles-permisos` y su detalle) dejaron de aceptar `view-user`; ahora exigen `manage-roles` o `edit-user`, el mismo criterio que ya aplican los endpoints de administración.
- **[fix]** Se retiró `view-user` del `authority` del grupo «Gestión» para que el título/collapse no aparezca vacío a roles sin subpáginas accesibles.
- **Decisión:** se optó por ocultar el módulo en vez de habilitar un modo solo-lectura. `view-user` está en Cobranza únicamente para poblar el selector de encargados (`assignee_ids`) de Pagos diferidos; el diseño no contempla que Cobranza administre usuarios, y el detalle dispara `/roles` y `/permissions`, que devolvían 403.
- **Verificación:** `tsc --noEmit` sin errores.
- **Referencia:** `src/config/pages.config.ts` (menú y rutas comparten el mismo `authority`).
- **Estado:** terminado.
- **[fix]** Se agregó el acceso a Cartera de crédito al aside Comercial y se habilitó el contenedor para `view-deferred-payment`, sin depender del nombre del rol.
- **[fix]** El modal de edición de cartera presenta el cupo en CLP sin decimales, preservando los centavos originales si el usuario no lo modifica.
- **[fix]** La cartera inicia y se reinicia por cambio de subsidiaria con 10 resultados por página.
- **[style]** Se homologaron filtros, estados, tabla vacía y paginación de la cartera con la experiencia de Pagos diferidos.
- **[style]** Las acciones de documentos y edición usan el patrón compacto con tooltip y color de Cotizaciones, conservando sus permisos y destinos.
- **[style]** Los montos negativos de cupo, usado o disponible se destacan en rojo sin alterar su formato CLP.
- **[fix]** Pagos diferidos carga el nombre del cliente como texto en la búsqueda al abrir documentos desde Cartera de crédito, sin aplicar `customer_sale_id`.
- **[fix]** La apertura de documentos sin razón social ni RUT ya no aplica el fallback “Cliente sin nombre”; la carga de cartera conserva el mensaje del API y no reutiliza una búsqueda pendiente al cambiar de subsidiaria.
- **[test]** Se agregó cobertura focal para el mensaje API y el cambio de subsidiaria con búsqueda debounceada; se corrigió el formato Prettier de las acciones de tabla y se actualizó la evidencia de lint del PR.
- **[fix]** Se restauró el debounce de la búsqueda en la primera página y se activó la carga antes de la consulta inicial o al cambiar de subsidiaria, evitando mostrar un estado vacío prematuro.
- **[test]** La cobertura de carga ahora captura el primer render con subsidiaria activa y descarta cualquier frame vacío sin carga al cambiar de subsidiaria tras haber cargado datos.

## Tema: Detalle de cliente — jerarquía visual

**Rama:** `feat/jerarquia-visual-detalle-cliente`

- **[style]** Reordenada la vista de detalle de cliente por identidad, comercial, logística, crédito y notas.
- **Decisión:** Se reutilizó la card de condiciones de crédito como referencia visual y se unificaron los estados vacíos sin cambiar datos ni endpoints.
- **[style]** Se corrigió el ancho de los campos para que identidad, comercial y logística ocupen toda su cuadrícula en lectura y edición.
- **[style]** Se homologó la grilla vertical de Facturación y Despacho para conservar la alineación en todos los breakpoints.
- **[style]** Se homologaron los encabezados de Facturación y Despacho para alinear el inicio de sus campos.
- **[style]** Los selectores sin valor muestran el placeholder localizado “Seleccionar”.
- **Estado:** Terminado; verificación focalizada y TypeScript aprobados.

## Tema: ZF-19 — título seguro del detalle de cliente

**Rama:** `fix/zf-19-titulo-detalle-cliente`

- **[fix]** El título del navegador se sincroniza al resolver el detalle, usa empresa, contacto o nombre del cliente y aplica `Detalle de cliente | ERP` como fallback seguro.
- **[test]** Se cubrieron la transición desde `Error | ERP`, la prioridad de identidad y el fallback sin datos.
- **Estado:** Terminado; sin cambios de API, rutas ni permisos.

<!-- #CachorroLouco -->

---

## Tema: ZF-14 — Vista de activación de usuario falla por hook condicional

**Rama:** `fix/user-activation-redux-provider-zf14` (desde `origin/develop`)

- **[fix]** `useForceLightMode()` se ejecuta antes de los retornos condicionales para preservar el orden de hooks y evitar que la vista quede en blanco.
- **[fix]** El fallback de `ErrorBoundary` usa botones nativos, por lo que no depende del contexto Redux si el error ocurre fuera del `Provider`.
- **[feat]** `activationError.util.ts` decide los mensajes por estado HTTP (404, 410, 422, 5xx o sin respuesta).
- **[test]** Se cubren los mensajes del utilitario y la regresión del orden de hooks.
- **Pendiente (backend):** el endpoint de activación debe devolver 410 para invitaciones usadas o expiradas en vez de 500.
- **Estado:** listo para PR hacia `develop`.
