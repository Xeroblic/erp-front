# 08-07-2026 — useOrgContextSwitcher

## Sesión — Centralización del cambio de contexto

**Rama:** `refactor/org-context-switcher` (basada en `origin/develop`, independiente del PR #73)

- `[refactor]` Nuevo `src/hooks/useOrgContextSwitcher.ts`: hook único para ejecutar el
  cambio de contexto organizacional. Expone `switchContext({ sucursalPrincipal,
  subsidiaryId?, companyId?, eventBranchId? })` e `isSwitching`.
  - Arregla el bug `company_id: 1` hardcodeado → resuelve el company_id REAL
    (`personalizacion.company_id ?? user.company.id`).
  - Emite un evento unificado `org-context-changed` bien tipado
    (`{ companyId, subsidiaryId, branchId }`) **+** el legacy `user-branch-changed` por
    compatibilidad, ahora con semántica CORRECTA (la subsidiaria ya NO viaja dentro de
    `branchId`).
- `[fix]` `useCompanyManager.switchCompany` delega en `useOrgContextSwitcher`. Elimina el
  `company_id: 1` y la mezcla subsidiaria→branchId que confundía a `useIngresoStock`
  (escucha `user-branch-changed` y actuaba sobre `detail.branchId`).

**Estado:** `tsc --noEmit` OK. Lint del hook nuevo consistente con el repo (import/extensions
es deuda repo-wide; console.warn es warning, igual que otros hooks).

**⚠️ Pendiente de verificar con la app corriendo** (cambia runtime del switch de
empresa/subsidiaria): probar el cambio desde `CompanySelectorButton` y confirmar que
`useIngresoStock` ya no recibe una subsidiaria como `branchId`.

**No migrado (a propósito):** `SelectSucursalEmpresa` ya resuelve bien el company_id y emite
el evento correcto, y usa `actualizarSucursalPrincipalThunk` (actualiza el slice de
personalización). Migrarlo al hook (que hace PUT directo) perdería esa actualización del
slice → regresión. Queda como follow-up si se unifica el hook con el thunk.
