# Bitácora — 24-07-2026

## Temas del día

- Correcciones de revisión del dashboard de pagos diferidos (ZF-5)

---

## Tema: Correcciones de revisión ZF-5

**Rama:** `feat/dashboard-pagos-diferidos` · **Issue:** ZF-5 · **PR base:** `feat/pagos-diferidos`

La rama base intermedia es deliberada: este PR incorpora únicamente el dashboard ZF-5 sobre la rama general de Pagos Diferidos. ZF-5 continúa abierto para recibir ajustes adicionales.

- **[fix]** Se declaró el modo mock con comparación explícita contra `true` y se agregó un aviso visible de datos ficticios.
- **[fix]** Redux quedó como fuente única de la búsqueda, evitando restaurar términos obsoletos después de limpiar.
- **[fix]** Los abortos dejaron de generar errores falsos y se separaron los errores de listado y resumen.
- **[fix]** Se alineó el guard de contexto con el modo mock y se evitó mezclar información entre subsidiarias.
- **[fix]** Se corrigieron el selector de estado, la semántica de filas, los badges y el contrato nullable de `days_until_due`.
- **[style]** Se mantuvo visible la paginación durante recargas normales y se deshabilitaron temporalmente sus controles.
- **[refactor]** `formatCLP` se movió a utilidades compartidas y se retiró código sin consumidores.
- **[test]** Se cubrieron carga mock, búsqueda, paginación, errores independientes, solicitudes vigentes y abortos.
- **[docs]** Se corrigió el nombre histórico de la rama del 23-07 a `feat/dashboard-pagos-diferidos`.

### Validación registrada

- TypeScript: `tsc --noEmit` sin errores.
- Suite global: 34 archivos y 180 pruebas aprobadas.
- ESLint focalizado: sin errores propios del módulo, descontando reglas globales preexistentes.

### Pendientes del backend

- Publicar nombres definitivos de permisos.
- Aceptar `overdue` separado de `status` en el contrato del listado.
- Formalizar `days_until_due` como nullable.
- Validar el rango de fechas en el FormRequest.
- Garantizar `personalizacion.subsidiary_id` en `/me`.

**Estado:** correcciones de primera pasada implementadas; ZF-5 permanece abierto.
