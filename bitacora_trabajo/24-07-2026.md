## Temas del dia

- Correcciones PR review: boton descargar fotos ZIP

---

## Tema: Correcciones PR review — boton descargar fotos ZIP

**Rama:** `feat/descargar-fotos-zip`

Aplicados fixes tras review del PR #108:

### Cambios

| Fix | Descripcion |
|---|---|
| `GallerySection.tsx` | Boton crudo reemplazado por `<Button>` del Design System |
| `useReviewPhotos.ts` | Manejo de errores blob: parsear `err.response.data.text()` para extraer mensaje real |
| `useReviewPhotos.ts` | `revokeObjectURL` envuelto en `setTimeout` para compatibilidad Firefox/Safari |
| `bitacora_trabajo/23-07-2026.md` | Corregida ruta en tabla de archivos |

### Decisiones

- Design System es ley: boton usa `<Button variant='outline' color='blue' isLoading={...}>`
- Errores con `responseType: 'blob'`: axios entrega el body de error como Blob, hay que hacer `await blob.text()` + `JSON.parse` para extraer `message`.
- `revokeObjectURL` con 100ms de delay para evitar que Firefox/Safari cancelen la descarga.

[fix]
