# 23-09-2026

## Temas del día

- Reportes de inventario: formato de exportación pedido al backend.
- Inventario: trazabilidad de los productos sin serie (pestaña y ficha de producto).
- Separación en ramas de los cambios sin commitear.
- Catálogo del contrato: componentes compartidos que faltaban.

---

## Tema: Formato de exportación de Reportes de inventario pedido al backend

**Rama:** `feat/reportes-inventario` (desde `feat/refactor-expansion-stock`)

- **Por qué:** los archivos PDF y Excel de las cuatro pestañas se generan en el navegador con un formato único. Se decidió que los genere el backend y que la versión del navegador quede como reemplazo provisional.
- **[docs]** Preguntas 19–22 en `Docs/abastecimiento-preguntas-backend.md`, con el diseño completo del formato: nombre del archivo, encabezado, indicadores y columnas por tipo, estructura del Excel y del PDF, filtros y orden de la exportación, un tipo exportable para Estadísticas (`inventory_statistics`) y una señal de versión del formato para el cambio. El contrato (`Docs/inventario-unificado-contrato.md`) apunta a esas preguntas.
- **[docs]** El generador del navegador (`export/reportDocument.ts`, `useLocalReportExport`) queda marcado como provisional y como referencia del formato pedido.
- **Estado:** diseño anotado; se lleva a la presentación con el resto de las preguntas. Pruebas, lint y PR pendientes.

---

## Tema: Trazabilidad de los productos sin serie en Inventario

**Rama:** `feat/trazabilidad-inventario` (desde `feat/reportes-inventario`)

- **Por qué:** Inventario mostraba cuánto hay y de dónde vino, pero no qué entró, salió o se movió
  ni con qué saldo quedó cada ubicación. Los productos con serie ya se siguen por número de serie;
  los sin serie no tenían historial.
- **[feat]** Pestaña «Trazabilidad» en Inventario (`?vista=trazabilidad`), con permiso
  `view-inventory-movements`. Muestra las operaciones de la sucursal (recepciones, reversiones,
  traslados, ajustes, documentación y saldo inicial), con filtros de búsqueda, tipo, ubicación y
  fechas en la URL. Cada operación se despliega con sus productos, la condición, el saldo de cada
  ubicación antes y después, y la procedencia; si hay un filtro activo, destaca los ítems que
  coinciden.
- **[feat]** Tarjeta «Trazabilidad» en la ficha de producto (sólo sin serie), con la misma lista
  filtrada por producto.
- **[feat]** Contrato §14 (`GET S/inventory-operations`) tipado y simulado: el mock arma la
  historia con el stock simulado y reproduce los saldos, así el último «después» coincide con el
  stock actual (comprobado con un traslado y un ajuste). El registro de recepciones del mock ahora
  guarda la bodega, la fecha y de dónde retiró cada reversión.
- **[docs]** Preguntas 23–26 al backend (ubicación en los efectos, exclusión de series, tipo del
  saldo inicial, unidades de las operaciones documentales) y vista agregada al contrato unificado.
- **[test]** Los mocks de `useCurrentBranch` en las pruebas de Inventario y de la ficha no traían `visibleBranches`, que ahora leen las vistas: 13 pruebas caían. Se agregó la lista vacía; 26/26 en verde.
- **[test]** 22 pruebas nuevas en 5 suites: mock del §14 (orden, unidades de un traslado, último «después» igual al stock actual, encadenado antes/después, series fuera, filtros y 422/404), propiedad de la lista y del detalle en el slice, filtros `tipo`/`desde`/`hasta` en la URL y su traducción al §14, y la compuerta `view-inventory-movements` en la pestaña y en la ficha. Las pruebas reinician el store de recepciones: su worker simulado (900 ms) publicaba la recepción encolada a mitad de otra prueba.
- **Estado:** revisión visual hecha (pestaña con filtros, detalle desplegado con saldo antes/después y procedencia, y tarjeta de la ficha), sin hallazgos; PR hacia `feat/reportes-inventario`.

---

## Tema: Separación en ramas de los cambios sin commitear

**Rama:** `feat/reportes-inventario` (origen del working tree)

- **Por qué:** el working tree de `feat/reportes-inventario` mezclaba cinco temas de días y ramas
  distintos; varios venían del stash `otros-cambios-temporal` y la bitácora los daba por
  commiteados o en PR sin que existiera el commit.
- **[chore]** Un commit local por tema, sin push:
    - `feat/reportes-inventario`: Reportes de inventario (22-09 y 23-09).
    - `feat/trazabilidad-inventario` (desde `feat/reportes-inventario`): trazabilidad (23-09).
    - `style/flecha-centrada-traslados` (desde `feat/refactor-expansion-stock`): flecha del
      traslado (16-09). La rama existía sin commits.
    - `style/campos-obligatorios-recepcion` (desde `feat/refactor-expansion-stock`): asterisco en
      los campos obligatorios del formulario de Recepción (el costo sólo con proveedor conocido),
      nota «Campos obligatorios», fondo blanco en todos los campos como en el documento de compra
      y borde gris en reposo del select de bodega. No tenía registro en la bitácora.
    - `fix/fechas-invalidas-pagos-diferidos` (desde `develop`): fechas inválidas en Pagos
      diferidos (21-09).
- **[fix]** Correcciones de la revisión previa: mocks de `useCurrentBranch` en las pruebas de
  Inventario (13 caían), orden de imports en `reportSlice`, export por defecto en
  `reportExcel`, y la edición del umbral en Reportes se ofrece sólo con datos simulados (antes
  dependía de la bandera y, con datos del backend, guardaba en el mock sin reflejarse).
- **Decisión:** `.claude/launch.json` (configuración local del navegador del panel) queda sin
  commitear. Respaldo de todo el working tree en el stash `respaldo-separacion-ramas-23-09`.
- **Estado:** ramas locales listas para revisión visual; push y PR pendientes de autorización.

---

## Tema: Catálogo del contrato al día con los componentes compartidos

**Rama:** `feat/catalogo-contrato-componentes` (desde `feat/refactor-expansion-stock`)

- **Por qué:** el catálogo (`/inventario/abastecimiento/catalogo`) quedó en lo que entregó la card
  01; cuatro piezas de `@/components/procurement` llegaron después y no se mostraban.
- **[feat]** Secciones nuevas: etiqueta de condición (apto, no apto y sin icono), píldora de estado
  (convención de color con una etiqueta real por color y ancho fijo frente a `fit`), miniatura de
  producto y aviso de datos simulados (aviso con la bandera encendida y pantalla no habilitada con
  la bandera apagada). El aviso inicial menciona la ampliación del contrato para Inventario.
- **Decisión:** el aviso «ningún endpoint existe todavía» se mantiene: Laravel Boost confirma que
  el backend no tiene aún las rutas `procurement/*` ni `inventory-stock`.
- **[test]** 2 pruebas nuevas en `useCatalogoContrato` (condiciones y los seis colores); 8/8,
  typecheck, eslint, prettier y `lint:imports` limpios.
- **Estado:** revisión visual hecha; PR hacia `feat/refactor-expansion-stock`.
