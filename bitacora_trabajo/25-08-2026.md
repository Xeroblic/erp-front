## Temas del día

- ZF-2: ausencia de RAM y disco en Revisiones Técnicas.
- ZF-2: consistencia visual del wizard (toggles dentro de tarjetas + paridad AIO).

## Tema: ZF-2 — ausencia de RAM y disco

- Rama: `feat/zf-2-ausencia-ram-disco`.
- [feat] Reemplaza el sentinel `0` por `has_no_ram` y `has_no_storage` en notebook, desktop y AIO; ajusta visualización, impresión y exportaciones.
- [fix] Corrige la revisión del PR #173: al marcar ausencia, los cinco campos dependientes se envían como `null`; el estado se muestra en sólo lectura, el switch conserva un nombre accesible estable, el mensaje de validación lista los campos requeridos y la limpieza se centraliza.
- Estado: correcciones commiteadas y publicadas en el PR #173 (HEAD `74744c32`).

## Tema: ZF-2 — consistencia visual del wizard de revisión técnica

- Rama: `fix/zf-2-toggles-en-tarjetas` (basada en develop con ZF-2 ya mergeado).
- [fix] Los switches «Tiene RAM» / «Tiene disco» dejan de flotar sobre la grilla: ahora viven en el header de su tarjeta (título a la izquierda, switch a la derecha). Al desactivar, la tarjeta permanece visible colapsada y atenuada mostrando «No tiene RAM/disco», con el switch operable para revertir. Nuevo componente compartido `HardwareCard` que encapsula header + colapso.
- [fix] Modo lectura: la ausencia ya no es texto plano; reutiliza la misma tarjeta colapsada sin switch (`HardwareAbsenceStatus` pasa a ser el contenido del cuerpo colapsado).
- [refactor] AIO adopta el layout de notebook/desktop: dos tarjetas (RAM azul / Almacenamiento morada), tipo de RAM por selección de tarjetas usando `RAM_TYPE_OPTIONS` de AIO, y uso del `SelectionCard` compartido eliminando el duplicado local naranja.
- [fix] Label de slots RAM en notebook mostraba «\tram Slots» por un tab dentro de la clave (`getNotebookLabel('\tram_slots')`).
- Comportamiento funcional intacto: limpieza de campos por `useHardwareAbsence`, validación condicional Yup y flujo «No enciende» sin cambios.
- Estado: implementado y verificado (vitest afectados 5 suites / 17 tests, `tsc --noEmit` 0 errores, eslint sin errores en archivos tocados); pendiente commit/PR por autorización del usuario.
