# Bitácora — 09-07-2026

## Temas del día

- Woo / ventas / cotizaciones / productos (tab sync y flujos)
- IAdminUser — separar usuario admin de IUserMe
- Consolidar usuarios admin (huérfana + IUserMe solo sesión)
- Fix sucursal_principal vs subsidiary_id
- Alias soup ISubempresa (contrato real backend)
- Alias soup ISucursal/IBranch (+ cierre IEmpresa)

---

## Tema: Woo / ventas / cotizaciones / productos (tab sync y flujos)

**Rama:** `fix/woo-sync-tab-ajustes` (desde `develop`)

Backend solo lectura; todo en front. `tsc --noEmit` OK.

### Woo — tab y scroll
- **[fix]** `Tabs`: `overflow-hidden` → `overflow-x-hidden` (scroll OffCanvas / tab
  "Publicacion Sincronizada WooErp").
- **[fix]** `CreateEditProductModal`: tabs también en producción (sin gate `DEV`).

### Ventas — cierre real vs estado Woo
- **[fix]** `ISale`: `is_closed`, `completed_at`; documentados `can_close` /
  `inventory_finalized`.
- **[fix]** `SaleDetailPage`: cerrar por `can_close`; badge por `is_closed` (no status Woo);
  "Finalizada" por `inventory_finalized`.

### Cotizaciones — estados y conversión
- **[feat]** Botones por estado: Aprobar → Generar Venta → Ir a Venta; sin editar convertida
  o con venta cerrada.
- **[feat]** Aprobar en lista para `sent`.
- **[fix]** Conversión real desde modal (`handleConfirmConvertToSale`).
- **[fix]** Reducer `convertQuoteToSale.fulfilled` con shape `{ message, sale }`.
- **[refactor]** `QuoteLinkedSale` tipado (sin `any`).

### Productos — Woo, variantes, filtros
- **[fix]** `normalizeProduct`/`normalizeChildren` preservan flags Woo.
- **[feat]** Columna/badge Woo por grado publicado; búsqueda client-side con variantes;
  filtro "Solo Woo"; rango fechas `updated_at` con Calendar pro.

**Estado:** código listo (4 commits); pendiente prueba browser + PR.

---

## Tema: IAdminUser — separar usuario admin de IUserMe

**Rama:** `refactor/admin-user-type` · **#9 slice 1**

- **[refactor]** `IAdminUser` (+ company/contextual role/branch) contrato de `GET /users`.
- **[refactor]** Módulo `gestionAdmin/usuarios` retipado `IUserMe` → `IAdminUser`.
- Campos de UI no presentes en `/users` como opcionales `@deprecated` (cero cambio runtime).

**Follow-up:** slices 2–3 (displays reales; adelgazar `IUserMe`) → tema consolidar.

---

## Tema: Consolidar usuarios admin (huérfana + IUserMe solo sesión)

**Rama:** `refactor/consolidate-admin-users` (con #81) · **#9**

- Hallazgo: `/gestion/usuarios` huérfana; viva es `RolesPermisos` con `UserWithDetails`.
- **[chore]** Borrada página huérfana (11 archivos) + ruta/menú + backup muerto.
- **[refactor]** `UserWithDetails extends IAdminUser` (ya no `IUserMe`); `IUserMe` = solo sesión.
- **[refactor]** Quitados `@deprecated` de `IAdminUser`; 8 consumidores a campos reales
  (`cargo`, `companies[].position`, etc.).

**Verificar:** `/gestion/roles-permisos`, managers en sucursales; `/gestion/usuarios` → 404.

---

## Tema: Fix sucursal_principal vs subsidiary_id

**Rama:** `refactor/consolidate-admin-users` (PR #83) · **#5 / issue #80**

- Diagnóstico corregido: backend ya persiste `subsidiary_id`; bug 100% front
  (`sucursalPrincipal: subsidiaryId`).
- **[feat]** `actualizarSubsidiariaPrincipalThunk` → `PUT { subsidiary_id }`.
- **[fix]** Switcher discrimina: `subsidiaryId` vs `sucursalPrincipal` sin mezclar.
- **[fix]** `useCompanyManager.switchCompany` solo pasa `subsidiaryId`.

**Verificar Network:** company switch → solo `subsidiary_id`; sucursal → solo
`sucursal_principal`.

---

## Tema: Alias soup ISubempresa (contrato real backend)

**Rama:** `refactor/subsidiary-contract` · **#13 módulo subsidiaria**

- Evidencia backend: solo `subsidiary_*`; alias camel eran fabricación del front.
- **[refactor]** `ISubempresa` sin `INormalizedEntityAliases`; `subsidiary_status: boolean`.
- **[refactor]** 14 consumidores migrados (tsc 48 → 0); normalizadores sin fabricar alias.

**Fuera de alcance aquí:** ISucursal/IBranch e IEmpresa (temas siguientes).

---

## Tema: Alias soup ISucursal/IBranch (+ cierre IEmpresa)

**Rama:** `refactor/branch-contract` (sobre subsidiary / PR #84) · **#13 / issue #77**

- **[refactor]** `ISucursal` sin alias soup; 6 consumidores; `normalizeBranchData` limpio.
- **[fix]** Columna Comuna en `Sucursales.tsx` vía `commune?.name` (antes siempre vacío).
- **[docs]** `IEmpresa` ya limpia — cierra slice 3 sin cambios.

**Cierra #13 / #77** tras merge #84 + esta rama.
**Verificar:** listado sucursales (comuna), selectores en categorías/productos/transferencias.
