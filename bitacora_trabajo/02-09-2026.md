# Bitácora — 02-09-2026

## Temas del día

- Segunda pasada de revisión del PR #184: contrato de bisagras, warnings del backend y deudas de test.
- ZF-98: puertos sueltos, cubierta del teclado y marcas de teclado en la pantalla (fase F3 del motor de graduación).
- Puertos de revisión técnica: los cinco tipos declaran los nueve contadores del catálogo, se agrega el puerto de carga, se renombran los dos de monitor y desaparecen los topes.
- Tercera pasada del PR #184: rótulos de las condiciones de bisagra y aviso repetido del autoguardado.
- Ajustes de captura de puertos: grilla 3x3 y las dos preguntas de estado arrancan en «No».

---

## Tema: Correcciones de la segunda pasada del PR #184

**Rama:** `feat/campos-estructurados-revision-tecnica`

- **[fix]** B1-bis (bloqueante) — La corrección de B1 restauró `HINGE_CONDITION_OPTIONS` como respaldo, pero esa constante seguía congelada en el contrato de componente genérico. Ofrecía `missing_pieces`, que `CONDITION_HINGE` rechaza, y escondía `cracked` y `loose`, los dos estados que ZB-89 creó para limitar a grado C. Una bisagra trizada o suelta sólo podía registrarse como `worn` (grado B) o `broken` (grado M). Se alinean `ALLOWED_HINGE_CONDITIONS`, `HINGE_CONDITION_OPTIONS` y la metadata local con `CONDITION_HINGE` = `ok, worn, cracked, loose, broken`, con los rótulos que publica el backend.
- **[fix]** `updateItemDetails` devolvía sólo `data` y descartaba `warnings`. La ruta de sucursal —la que usa cualquier técnico con sucursal activa— valida con `Arr::only`: persiste lo que pasó, descarta el resto y responde **200** con los rechazos en esa clave. El autosave tomaba ese 200 como éxito, congelaba el snapshot **con el valor inválido dentro** (así que no reintentaba nunca) y el badge mostraba «Guardado». Ahora el thunk rechaza con el mensaje del backend: el autosave reintenta con el payload saneado y, si tampoco pasa, el técnico ve el error. Afecta a todos los campos, no sólo a bisagras.
- **[fix]** `ITechnicalReviewSchemaField.required` estaba declarado y no lo leía nadie, pese a que el backend lo publica en los cinco campos de ZF-48. `resolveSchemaField` lo expone con respaldo local (lo que exige `notebookSchema`) y la UI dibuja el asterisco a partir de él: antes estaba fijo en el teclado y nunca aparecía en bisagras, que también es obligatorio.
- **[fix]** `NOTEBOOK_SECTION_FIELDS.input` declaraba `speakers_condition` y `non_functional_keys_count`, que `InputSection` sólo renderiza si el schema remoto los publica. La validación por sección ahora los excluye cuando no están en pantalla, para no dejar al técnico ante un error sin campo que corregir.
- **[test]** La aserción de `zf48SchemaFields` recolectaba `getAllByRole('radio')` de toda la sección y comprobaba sólo la unión: no probaba nada por campo y fijaba en verde `missing_pieces` como opción válida de bisagras. Cada grupo de opciones pasa a ser un `radiogroup` rotulado —que además es el marcado correcto para radios— y la aserción se acota a cada campo.
- **[fix]** Residuo del punto anterior: cuando el reintento del autosave sí logra guardar porque el sanitizador quitó el valor rechazado, el campo desaparece igual y el badge vuelve a decir «Guardado». `useAutoSave` compara ahora el payload crudo con el saneado y, si el reintento dejó de enviar campos, avisa cuáles quedaron sin registrar. El aviso sale también en los guardados silenciosos: lo silencioso es el éxito, no la pérdida de un dato.
- **[feat]** `getEquipmentFieldLabel` en `translations/equipment.labels.ts`: cada tipo de equipo ya tenía su mapa y su getter, pero no había forma de rotular un campo cuando el tipo llega en tiempo de ejecución, que es el caso del autosave. El aviso nombra «Bisagras», no `hinge_condition`.
- **[test]** Cobertura nueva: los tres caminos de `warnings` en el thunk (con mensaje, sin mensaje y respuesta limpia), el contrato de bisagras en el sanitizador sin schema remoto, y `required` efectivo (remoto manda, respaldo local cuando el backend no lo publica).
- **Decisión:** los rótulos del respaldo local se copian de los que publica el backend (`Trizada`, `Suelta`, `Rota`), para que schema remoto y respaldo describan el mismo estado.
- **Fuera de alcance, confirmado con el revisor:** `powers_on` sin UI en notebook (el alcance declarado del PR lo asigna a Desktop; hoy `TECHNICAL_REVIEW_ENFORCE_COMPLETION_REQUIREMENTS` está en `false`) y las opciones que el backend acepta pero no puntúa, que se corrigen del lado del backend.
- **[test]** Los dos caminos del aviso: reintento que descarta un campo (avisa una vez, con el rótulo) y reintento que envía lo mismo (no avisa). Sin el segundo, el aviso podría dispararse ante un fallo de red y quedar en falso positivo.
- **Estado:** `tsc --noEmit` en 0 errores; suite completa **102 suites / 561 pruebas** en verde (antes 101/553); alcance del módulo **13 suites / 51 pruebas** (antes 12/43). `eslint` sobre los archivos tocados: mismos hallazgos antes y después, todos preexistentes — ninguno nuevo. `check:eol`, `prettier --check` y `git diff --check` limpios. Cada fix se verificó revirtiendo su línea y confirmando la prueba en rojo.
- **Pendiente:** las pruebas manuales sugeridas por la revisión (red lenta sobre la sección «Entrada», bloqueo de `**/validation/rules/notebook`, grado sugerido para `cracked`/`loose`/`worn`, recorrido de lote contando peticiones al schema).

---

## Tema: ZF-98 — puertos sueltos, cubierta del teclado y marcas de teclado

**Rama:** `feat/zf-98-puertos-sueltos-cubierta-teclado` (apilada sobre `feat/campos-estructurados-revision-tecnica`)

Contraparte de frontend de la fase **F3** del plan de corrección del motor de graduación. Salió del análisis de producción: de 191 notebooks con un puerto suelto mencionado en observaciones, ninguno quedó en grado A o B, y no existía el campo donde registrarlo.

- **[feat]** Donde antes había una sola pregunta —«¿Todos los puertos funcionan?»— ahora hay dos independientes: «¿Hay puertos defectuosos?» y «¿Hay puertos sueltos?». Un puerto suelto no cabía en la pregunta anterior: responder «sí funcionan» era verdad y aun así dejaba el defecto sin registrar. Cada pregunta abre su propio desglose.
- **[feat]** El desglose se captura con **un contador por tipo de puerto**, igual que la grilla de cantidad de puertos del equipo. `PortTypeCounters` (nuevo) y `PortConditionFields` (nuevo, compartido) reemplazan cinco implementaciones divergentes de la sección de puertos por una sola.
- **[feat]** `keyboard_cover_condition` (palmrest) en notebook, como control propio junto a la tapa superior y las bisagras. Ojo con el orden de gravedad: acá `cracked` es más leve que `broken`, al revés que en la bisagra. Lo confirmó el técnico y no se empareja.
- **[feat]** `keyboard_marks` como valor nuevo de `screen_condition` en notebook. `screen_condition` pasa a resolverse desde el schema con las constantes locales de respaldo, así que la opción aparece **sólo** si el backend con F3 la publica; contra un backend sin la fase el técnico no puede elegir un valor que da 422. Mismo criterio que ZF-48 aplicó a `speakers_condition`.
- **[refactor]** Migración al contrato de mapa del backend (`92b0cc8`): `loose_port_types` y `defective_port_types` pasaron de lista de tipos (`['hdmi','usb_c']`) a mapa `{tipo: cantidad}` (`{"hdmi": 2}`). La forma anterior ahora devuelve 422.
- **[fix]** Las revisiones ya guardadas traen la lista vieja desde la base. `normalizePortTypeCounts` la convierte al hidratar —contando repeticiones— para que el formulario nunca sostenga la forma que el backend rechaza. Sin eso, abrir una revisión existente rompía el primer autosave.
- **[fix]** `loose_ports_count` lo deriva el servidor (el schema lo marca `derived_from` y su hint pide no enviarlo), pero sigue en el estado del formulario porque se hidrata desde la respuesta. `filterTechnicalReviewPayload` lo quita del payload: es el único punto por el que pasan tanto el autosave como el guardado final.
- **[fix]** El resumen lateral mostraba «-» sobre un desglose que el técnico sí había llenado: `translateValue` caía en la rama genérica de objeto. Ahora lo renderiza como «HDMI (2), USB-C (1)», y el guardián exige que las claves sean del catálogo para no confundir cualquier objeto con un número adentro.
- **[fix]** El contenedor de sueltos aparecía debajo de la pregunta por los defectuosos cuando sólo se activaba ese: cada pregunta y su desglose viven ahora en la misma columna. Hay una prueba que fija esa relación de anidamiento, verificada rompiéndola a propósito.
- **[feat]** `useReviewValidationSchema` cubre los cinco tipos (antes sólo notebook y desktop) y normaliza el alias `all-in-one`; el aviso de carga y reintento del schema deja de ser exclusivo de esos dos. El thunk ya cachea por TTL y deduplica, así que revisar un lote no multiplica las peticiones.
- **[feat]** `StepperInput` acepta `label` y `disabled`. Una grilla de contadores repetía «Incrementar» nueve veces y quien navega con lector de pantalla no podía distinguirlos; además el modo lectura no bloqueaba los botones.
- **Decisión:** los cuatro campos quedan **opcionales**. El ticket dice que bloquean `complete-review` con 422, pero `TechnicalReviewItemHelpers::COMPLETION_REQUIREMENTS` no incluye ninguno y todos son `nullable`. Exigirlos en el formulario bloquearía revisiones que el backend sí acepta. Queda anotado para corregir el ticket.
- **Decisión:** `defective_ports_count` se calcula en el cliente a partir de su desglose. A diferencia del de sueltos, el backend no lo marca como derivado y lo sigue esperando como campo propio; calcularlo evita que el total contradiga al detalle.
- **Decisión:** `{}` es «se midió, ninguno» y omitir el campo es «no se midió», tal como define el contrato. La diferencia entre «sí, todavía sin marcar nada» y «no» es sólo de pantalla, así que vive en el componente y no inventa un puerto que el técnico no marcó.
- **Decisión:** en monitor el campo se guarda pero no puntúa (`LOOSE_PORT_SCORED_TYPES` lo excluye). El copy sale del schema, que ahí manda otro hint y ningún `warning`, así que la UI no anuncia techo de grado. No es un bug del formulario.
- **Decisión:** los campos con opciones remotas pierden el `oneOf` local (patrón que ya trajo ZF-48 con teclado, touchpad y bisagras). Dos fuentes de verdad rechazarían un valor que el backend sí publica.
- **[fix]** El autoguardado fallaba con «no se pueden guardar más de 10 puertos». El schema acota cada tipo (`value_max: 10`) pero no la suma, y el backend valida `defective_ports_count` con `max:10`: un desglose de once puertos daba 422 sin que ningún contador se viera fuera de rango. `PortTypeCounters` acota cada contador por el presupuesto que queda del total y avisa al llegar al tope.
- **[feat]** Se quita **DVI** del catálogo de puertos, tanto del desglose de sueltos/defectuosos como de la grilla base del monitor. El backend lo sigue publicando en `options` para otros clientes, así que las opciones remotas se filtran contra el catálogo local (`filterPortOptions`). Contrapartida asumida: un tipo nuevo del backend no aparece hasta agregarlo a `ALLOWED_PORT_TYPES`.
- **Decisión:** `dvi_ports` sigue declarado en el schema Yup del monitor y en la interfaz aunque el formulario ya no lo dibuje. Las revisiones que lo tengan guardado lo conservan en el estado y lo devuelven tal cual, en vez de perderlo en el primer autoguardado.
- **[fix]** La grilla base llegaba a 12 puertos por tipo (16 en aio, docking y monitor) y el desglose de defectuosos/sueltos sólo a 10: se podían declarar más puertos buenos de los que después se podían marcar como malos. Ahora los dos lados comparten `MAX_PORT_TYPE_COUNT`, **subido a 20** por pedido de operaciones; el aumento equivalente ya está solicitado al backend y entra antes de producción.
- **Decisión:** el tope de puertos es lo único que **no** sigue al schema remoto. El backend todavía publica `value_max: 10`, pero esa validación está por retirarse y acotar el formulario a diez le impediría al técnico registrar los puertos que el equipo realmente tiene. Consecuencia mientras tanto: un total mayor a diez vuelve con 422 en el autoguardado, hasta que el backend despliegue el cambio. Las opciones y los rótulos sí los sigue tomando del schema.
- **[style]** Se quitan de los contenedores los hints de `loose_port_types` y `defective_port_types` («Cantidad de puertos que no funcionan por tipo…» y «Cantidad de puertos sueltos por tipo, por ejemplo {"hdmi": 2}…»): describen el contrato del backend, no lo que el técnico tiene que hacer, y uno de ellos mostraba JSON en pantalla. Los hints de `loose_ports_count`/`defective_ports_count`, que sí anuncian el techo de grado, se mantienen.
- **[feat]** Los contadores de la grilla base ahora llevan `label`, igual que los del desglose: siete botones «Incrementar» idénticos no se distinguen con lector de pantalla.
- **Estado:** `typecheck` en 0 errores; alcance del módulo **14 suites / 151 pruebas** en verde (suite relacionada completa: 74 suites / 486). `eslint` acotado a las líneas tocadas: **0 hallazgos nuevos** (los tres `jsx-props-no-spreading` que aparecen en `forms/index.tsx` ya estaban en las cinco ramas del `switch` antes del cambio; sólo se movieron de línea). `prettier --check`, `check:eol`, `lint:imports` y `git diff --check` limpios.
- **Pendiente (backend, bloquea producción):** quitar (o subir a 20) el tope de puertos — `defective_ports_count`, `defective_ports_critical_count` y `loose_ports_count` con `max:10`. Hasta que salga, un desglose de más de diez puertos falla el autoguardado con 422. El formulario ya ofrece veinte.
- **Pendiente:** verificación manual contra un backend con F3 desplegado (el local ya está en `feat/technical-review-scoring`): que el schema publique los cinco campos, que el desglose sobreviva al guardado parcial y vuelva al recargar, y que el grado baje a M con 2 puertos sueltos en notebook y **no** cambie en monitor.
- **Pendiente (necesita backend):** agregar **puerto de carga** a la grilla base de cantidad de puertos. No existe columna `charging_ports` en ninguno de los cinco tipos: el backend sólo persiste las claves que valida, así que un contador nuevo se descartaría en silencio. `charging` sí existe en el catálogo del desglose (`PORT_TYPES`), que es de dónde viene la asimetría.
- **Fuera de alcance:** columnas de Excel en `excel.constants.ts` — el ticket no las pide. También queda sin tocar el DVI del editor de atributos del catálogo de productos (`AttributesTab/sections/PortsSection.tsx`), que responde a otro contrato y a otro módulo.

---

## Tema: Puertos de revisión técnica — catálogo único, renombres de monitor y sin topes

**Rama:** `feat/zf-98-puertos-sueltos-cubierta-teclado`

Contraparte del commit `0c318b6` del backend (`feat/technical-review-scoring`, migración
`2026_09_02_000001`, aplicada en desarrollo y **sin desplegar**). El backend no sale hasta que el
front tenga el renombre listo: la API ignora los nombres viejos en silencio —responde 200, sin
advertencias, y el campo queda en 0—, así que un monitor guardado con el formulario anterior
registraría puertos que no quedan en ninguna parte.

- **[feat]** `charging_ports` («Puertos de carga») en los cinco formularios. Era el pendiente que la
  entrada anterior dejó anotado como «necesita backend»: la columna ya existe en los cinco tipos.
- **[feat]** DVI vuelve al catálogo: `dvi_ports` como contador en notebook, desktop, docking y aio
  (en monitor ya existía), y `dvi` como tipo del desglose de sueltos y defectuosos. Se revierte la
  decisión de la entrada anterior de filtrarlo: el backend genera ahora los contadores y el desglose
  desde un solo mapa (`PORT_TYPE_COUNTERS`), así que ofrecer menos tipos de un lado que del otro es
  exactamente la asimetría que ese commit eliminó.
- **[feat]** `sd_readers` en monitor, que era el único tipo sin lector SD.
- **[fix]** Renombre en monitor, lo único que bloquea el despliegue del backend:
  `usb_hub_ports` → `usb_a_ports` y `type_c_ports` → `usb_c_ports`. La migración arrastró los datos
  (94 revisiones con hub USB, 85 con Tipo C), así que no hay backfill pendiente. Se quitan también
  el alias que `MonitorForm` sincronizaba en el submit final y el rótulo duplicado «Puertos Tipo C».
  Queda una carga defensiva de los nombres viejos al hidratar, sólo para una respuesta en caché o
  una pestaña abierta desde antes del despliegue.
- **[refactor]** `PORT_COUNTER_FIELDS` (espejo local de `PORT_TYPE_COUNTERS`) es la única fuente del
  orden, el nombre de columna y el rótulo de los nueve contadores; las cinco secciones arman su
  grilla desde ahí en vez de listar los puertos una por una. El orden es el del catálogo, que es el
  que devuelve el schema: VGA, HDMI, DisplayPort, DVI, USB-A, USB-C, lector SD, RJ45, carga.
  `ports.visuals.ts` guarda aparte el icono y el color de cada tipo, que son presentación.
- **[fix]** Desaparecen los topes. El backend retiró el `max:10` de `defective_ports_count`,
  `defective_ports_critical_count`, `loose_ports_count` y las cantidades por tipo de los dos mapas,
  y dejó de publicar `value_max`. Con eso se cae también el presupuesto total que `PortTypeCounters`
  administraba y su aviso «Máximo 20 puertos en total»: doce USB-A es una entrada válida. Queda un
  único techo de cordura, `MAX_PORT_COUNT`, en el límite real de la columna (`smallint`, 32.767),
  para no mandar un valor que la base rechazaría por desbordamiento.
- **Decisión:** el tope vuelve a seguir al backend, al revés que en la entrada anterior. Aquel
  `MAX_PORT_TYPE_COUNT = 20` existía para no quedar por debajo de lo que operaciones necesitaba
  mientras el backend seguía validando diez; retirada esa validación, sostener un tope propio sólo
  volvería a rechazar equipos reales.
- **[test]** Dos pruebas nuevas que fallan al revertir el fix, verificadas rompiéndolo a propósito:
  la grilla de los cinco tipos cuenta los nueve puertos del catálogo (fijado al número del contrato,
  no a `PORT_COUNTER_FIELDS.length`, que sería compararlo consigo mismo), y monitor escribe
  `usb_a_ports`/`usb_c_ports` sin ofrecer ya «Tipo C» ni «USB Hub». Se actualizan las que fijaban la
  semántica reemplazada: el recorte a diez, el presupuesto total y el filtrado de DVI.
- **Rutas de escritura recorridas (las cuatro):** submit final y los dos autoguardados leen el mismo
  estado del formulario, que ya sostiene los nombres nuevos; el preset «No enciende» de monitor se
  actualizó campo por campo y está cubierto por el tipo — con `usb_hub_ports` el `typecheck` falla
  con TS2561, comprobado.
- **Estado:** `typecheck` en 0 errores; suite completa **105 suites / 674 pruebas** en verde (antes
  105/667). `eslint` sobre los archivos del diff: **0 hallazgos nuevos** (los preexistentes de
  `MonitorForm`, `MonitorScreenSection`, los schemas y `reviewTranslations` dan el mismo conteo
  antes y después). `prettier --check`, `check:eol`, `lint:imports` y `git diff --check` limpios.
- **Pendiente:** verificación manual contra el backend con la migración aplicada — que el schema
  publique los nueve contadores con rótulo, que un monitor guarde USB-A y USB-C bajo el nombre nuevo
  y que un desglose de más de diez puertos ya no vuelva con 422.
- **Fuera de alcance:** las columnas de Excel (el monitor nunca exportó hub USB ni Tipo C) y el
  editor de atributos del catálogo de productos, que persiste su propio `attributes_json`. De ese
  editor sólo se ajusta la lista de campos que **importa** desde una revisión, porque los dos
  nombres viejos ya no pueden llegar.
- **Deuda detectada, no tocada:** los nombres de icono de las grillas de puertos
  (`UsbSymbol`, `DeviceTv`, `SdCard`, …) no resuelven contra `Icon`, que sólo carga `svg-icons/*` o
  los prefijos `Duo`/`Hero`: hoy no se dibuja ningún icono en esas tarjetas. Es previo a este cambio
  y afecta a las cinco secciones.

---

## Tema: Correcciones de la tercera pasada del PR #184

**Rama:** `feat/zf-98-puertos-sueltos-cubierta-teclado` (se integra al PR #184)

Revisión del 02-09 17:44 UTC sobre `86c9e8a2`: sin bloqueantes, con las cinco correcciones de la
segunda pasada cerradas y verificadas, y dos deudas nuevas.

- **[fix]** D1 — `HINGE_CONDITION_LABELS` había quedado en el contrato anterior de bisagras
  (`missing_pieces`, sin `cracked` ni `loose`), así que el técnico elegía «Trizada» en la tarjeta y
  el panel de resumen —y el texto de «Copiar info»— le devolvían `cracked` en crudo. Ahora el mapa
  se **deriva** de `HINGE_CONDITION_OPTIONS` en vez de escribirse aparte: es la misma clase de
  desincronización que el catálogo de puertos, y derivarlo la cierra de raíz.
- **[fix]** D1-bis — Alinear ese mapa destapaba el defecto de fondo: `VALUE_TRANSLATIONS` es plano y
  `ok`, `worn` y `broken` los comparten casi todas las condiciones, así que la última fusión ganaba
  y un solo rótulo describía la bisagra, la tapa, el teclado y el touchpad a la vez, con el género
  de uno solo. Unificar `broken` como «Rota» hubiera dejado «Touchpad: Rota». `translateValue` acepta
  ahora el nombre del campo —que los dos puntos de uso del resumen ya tenían a mano— y consulta
  primero el mapa de ese campo; el plano queda de respaldo para lo que no depende del campo.
  De paso queda cubierta la cubierta del teclado de ZF-98, cuyo `cracked` también salía en crudo,
  con rótulos propios sin el techo de grado que anuncia su tarjeta.
- **[fix]** D2 — El reintento saneado guardaba **su** snapshot, pero tanto `hasPendingChanges` como
  la guarda de `saveNow` comparan contra el payload crudo, que conserva el campo rechazado: los dos
  nunca coincidían, así que cada ciclo de inactividad repetía los dos PATCH —el que falla y el
  reintento— y volvía a avisar, indefinidamente. El snapshot que queda pasa a ser el crudo: el ciclo
  se detiene hasta que el técnico cambie el dato, que es cuando vuelve a tener sentido reintentar.
  Se eligió esta de las dos correcciones que ofrecía la revisión porque además corta los dos PATCH
  por ciclo, no sólo el aviso.
- **[test]** `reviewTranslations.test.ts` (nuevo): recorre las opciones de bisagra que ofrece el
  formulario, fija que el rótulo del campo mande sobre el compartido (`broken` es «Rota» en la
  bisagra y «Roto» en el teclado) y que el mapa plano siga siendo el respaldo. Reemplaza la prueba
  desechable que usó la revisión.
- **[test]** `useAutoSave`: tres `saveNow` consecutivos sobre el mismo dato rechazado dan **2
  dispatch y 1 aviso**, no 6 y 3.
- **Estado:** `typecheck` en 0 errores; suite completa **106 suites / 679 pruebas** en verde.
  `eslint` sobre los 35 archivos modificados: **48 errores / 4 warnings antes y después** — 0 nuevos;
  los dos archivos nuevos, sin hallazgos. `prettier --check`, `check:eol`, `lint:imports` y
  `git diff --check` limpios. Las dos correcciones se verificaron revirtiendo su línea y confirmando
  las pruebas en rojo (4 rojas).

---

## Tema: Ajustes de captura de puertos pedidos por operaciones

**Rama:** `feat/zf-98-puertos-sueltos-cubierta-teclado` (se integra al PR #184)

- **[style]** Los nueve contadores del catálogo se muestran en un **3x3** en los cinco
  formularios. Cada sección traía su propia grilla —cuatro columnas en notebook, desktop, aio y
  docking; cinco en monitor—, así que los nueve puertos caían en dos filas desparejas y la última
  quedaba a medio llenar. En pantalla angosta bajan a dos columnas para no aplastar el stepper.
  El desglose de sueltos y defectuosos ya usaba tres columnas.
- **[feat]** «¿Hay puertos defectuosos?» y «¿Hay puertos sueltos?» arrancan respondidas en **«No»**,
  que es el caso normal, y **respondidas de verdad**: el valor se escribe en el formulario, no sólo
  en la pantalla. Sin valor, `all_ports_functional` y `loose_port_types` no viajan en el PATCH
  —`filterTechnicalReviewPayload` descarta lo indefinido—, el servidor conserva lo que tuviera y el
  técnico se lleva la impresión contraria a lo que está viendo.
- **Decisión:** el arranque cubre sólo lo que llega sin medir. Una revisión ya respondida conserva
  la suya, y en modo lectura no se escribe nada. **Consecuencia asumida:** abrir una revisión vieja
  que nunca respondió esas preguntas ahora persiste «No» en el siguiente autoguardado, es decir,
  afirma que todos los puertos funcionan. Es lo que significa el valor por defecto pedido; si se
  prefiere acotarlo a revisiones nuevas, hay que decirlo.
- **[test]** Dos pruebas por tipo de equipo, verificadas revirtiendo el fix (10 rojas): las dos
  preguntas arrancan marcadas en «No» con el valor escrito en el formulario, y una revisión que ya
  traía respuesta no se pisa. Se actualizó la que fijaba `all_ports_functional` en `undefined` tras
  registrar un puerto suelto: ahora comprueba que sigue en `true`, que era su intención — registrar
  un suelto no obliga a declarar defectuosos.
- **Estado:** `typecheck` en 0 errores; suite completa **106 suites / 689 pruebas** en verde.
  `eslint` sobre los 7 archivos tocados: **0 errores antes y después**. `prettier --check`,
  `check:eol`, `lint:imports` y `git diff --check` limpios.
