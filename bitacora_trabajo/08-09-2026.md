# Bitácora — 08-09-2026

## Temas del día

- ZF-106 — Fundaciones del contrato de abastecimiento
- ZF-107 — Maestro de proveedores
- ZF-108 — Documentos de compra (boleta y factura)
- ZF-109 — Adjuntos privados del documento de compra
- ZF-101 — Los iconos de las tarjetas de puertos no se dibujan
- ZF-102 — Quinta pasada de revisión del PR #190 y corrección del rol ARIA de `YesNoSelector`
- Atajo «Equipo NO ENCIENDE» inutilizable en desktop
- Preparación del release v2.4.0 y promoción de `develop` a `main`

---

## Tema: ZF-106 — Fundaciones del contrato de abastecimiento

**Rama:** `feat/fundaciones-contrato-abastecimiento`

Primera card del proyecto Linear «refactor + expansión stock»: fijar los objetos que se
repiten en las ocho cards del módulo (producto, proveedor, documento, bodega y costo) como
tipos, fixtures y componentes compartidos, para que las siguientes los consuman en vez de
redibujarlos.

- **[feat]** Tipos TS de la sección 2 del contrato en `src/interface/procurement.interface.ts`:
  ficha completa de producto, compactos de proveedor/documento/bodega, bloque `cost`,
  `allowed_actions`, envoltorio `{data, context, links, meta}` y paginación (defecto 15,
  máximo 100).
- **[feat]** Fixtures en `src/mocks/db/procurement.db.ts`, copiados literalmente de los
  ejemplos de `frontend-guide.md`: costo neto, bruto, declarado, desconocido, ponderado y
  agregado mixto; producto serializado, no serializado e inactivo sin costo.
- **[feat]** Componentes compartidos en `src/components/procurement/`: `ProductCard`,
  `CostBlock` (lectura), `CostInput` (escritura: monto y base, nada más), `WarehouseLabel`
  («Sin ubicación») y `AllowedActionsToolbar`.
- **[feat]** Infraestructura transversal: mapa de errores de la sección 16
  (`procurementErrors.util.ts`), cabeceras `Idempotency-Key` / `If-Match`
  (`procurementWrite.util.ts` + `useIdempotentWrite`), aritmética decimal sin float
  (`procurementDecimal.util.ts`) y helpers de paginación.
- **[feat]** Pantalla de catálogo navegable en
  `/inventario/abastecimiento/catalogo`, con cada componente en sus estados. No hay
  Storybook en el repo, así que el catálogo cumple ese criterio de cierre.
- **[test]** 99 pruebas nuevas en 10 archivos: aritmética half-up y previsualización del IVA, mapa de
  errores, reuso de la clave de idempotencia, conformidad de los fixtures con el contrato
  y comportamiento de los cinco componentes.
- **Decisión:** `allowed_actions` se resuelve **por recurso**, no por nombre de acción:
  `update` exige `edit-procurement-supplier`, `edit-purchase-document` o `edit-product`
  según venga de proveedor, documento o recepción. Un registro global le habría dado a la
  recepción el permiso del documento.
- **Decisión:** El costo desconocido se muestra como desconocido y nunca como `$0`, y el
  agregado con `effective_basis: "mixed"` se distingue del de base única. Ambas cosas están
  cubiertas por prueba.
- **[docs]** `Docs/abastecimiento-preguntas-backend.md` con las 18 preguntas al backend que
  surgieron al modelar el contrato: es el entregable de cierre que pide el proyecto para la
  presentación.
- **Nota:** El módulo es **mock**. Ningún endpoint existe: el PR #67 del backend es sólo
  documentación y sus 22 tareas siguen pendientes.
- **[fix]** Correcciones de revisión sobre la misma card:
    - El costo opcional aceptaba mal la ausencia. Formik normaliza `''` a `undefined` antes de
      validar, pero un campo con espacios llega sin tocar; el schema comparaba contra `''` y
      rechazaba el costo desconocido, que es un estado válido del contrato.
    - `link_purchase_document` comprobaba un solo permiso. La sección 15 exige `edit-product`
      **y** `view-purchase-document`; la garantía queda en el componente compartido y no en
      que cada pantalla agregue otro guard.
    - Un timeout devolvía instrucciones contradictorias: `action: fix_input` («corrige los
      datos», que implica clave nueva) junto a `canRetry` («reintenta con la misma clave»). La
      clasificación se centralizó en `procurementErrors.util.ts`, que ahora resuelve el fallo
      de transporte como `retry_same_key` y deja de presentar el código de Axios como si fuera
      un código estable del contrato.
- **[test]** 11 pruebas nuevas, todas verificadas por mutación: revierten el fix y se ponen en
  rojo. Las del costo opcional usan Formik real, porque fuera de Formik el defecto no se
  reproduce.
- **Estado:** Implementación terminada y verificada localmente. Falta publicación si se
  autoriza.

---

## Tema: ZF-107 — Maestro de proveedores

**Rama:** `feat/maestro-proveedores`, sobre `feat/fundaciones-contrato-abastecimiento` (ZF-106,
`#195`, aún no fusionado). Segunda card del proyecto «refactor + expansión stock», bloqueada por
ZF-106 y bloqueante de ZF-108 (documentos de compra, que necesita el selector de proveedor).

Mockear el maestro de proveedores comerciales de compras (sección 5 del contrato): listado con
búsqueda y filtro de estado, ficha con resumen de compras, alta y edición, desactivación,
restauración y el conflicto por RUT duplicado. Sigue siendo mock — ningún endpoint existe — y
consume íntegra la infraestructura de ZF-106 (`useIdempotentWrite`, `resolveProcurementError`,
`AllowedActionsToolbar`, paginación).

- **[feat]** Tipos de la ficha completa (`IProcurementSupplier`, con `allowed_actions` y
  `purchase_summary`), la fila resumida sin resumen de compras, el payload de alta/edición y el
  conflicto de RUT, en `src/interface/procurement.interface.ts`. Nombrados
  `IProcurementSupplier*` a propósito: `ISupplier` ya existe dos veces en el repo para la entidad
  de revisión técnica, y este módulo no la mezcla ni la migra.
- **[feat]** Servicio mock en `src/services/procurement/procurementSuppliers.service.ts`: simula
  las seis rutas de `/procurement/suppliers` contra un store en memoria, con el mismo envoltorio,
  los mismos códigos de error y la misma garantía de `Idempotency-Key` (replay con la misma
  clave/payload, `409 IDEMPOTENCY_KEY_REUSED` con payload distinto) que tendrá la llamada real.
- **[feat]** Slice `src/store/slices/procurement/procurementSuppliersSlice.ts`: listado paginado,
  ficha (`current`), y las cuatro mutaciones — sin refetch tras editar/desactivar/restaurar,
  reflejando el resultado directo en `items` y `current`.
- **[feat]** Pantallas en `src/pages/inventario/abastecimiento/Proveedores/`: listado (alta, ver,
  desactivar, restaurar) y `ProveedoresDetalle/` (ficha completa, resumen de compras, botonera
  derivada de `allowed_actions`, edición). Rutas y menú registrados en `pages.config.ts`,
  `contentRoutes.tsx` y `DefaultAside.template.tsx` con `view-procurement-supplier`.
- **Decisión:** «Editar» no vive en la fila del listado. El formulario de edición necesita la
  ficha completa (direcciones, comunas, giro) y la fila resumida del contrato **no la trae** a
  propósito — pedirla por fila sería el resumen de compras caro que la sección 5 prohíbe. Editar
  vive en la ficha, que ya tiene los datos cargados.
- **Decisión:** El conflicto de RUT (409 `SUPPLIER_RUT_ALREADY_EXISTS`) se resuelve con un banner
  en el propio modal, no con un toast: `existing_supplier` se agregó a
  `IProcurementResolvedError` en `procurementErrors.util.ts` (única extensión a la infraestructura
  compartida de ZF-106) para leer esa excepción de compatibilidad del contrato sin envoltorio.
  Restaurar desde ese banner es un botón aparte — nunca un efecto lateral de guardar.
- **[fix]** El fixture `inactiveSupplier`/`inactiveSupplierFull` (`77888999-K`, literal del
  contrato) no es un RUT chileno válido — el DV real de `77888999` es `4`. La validación de la
  card 02 lo exige, así que el fixture se corrigió a `77888999-4` con nota explicando por qué.
- **[test]** 36 pruebas nuevas en 5 archivos: servicio mock (filtros excluyentes, obligatorios del
  formulario, conflicto de RUT con y sin restaurar, idempotencia), slice (flags y reflejo sin
  refetch), filtro de estado del listado, y el camino completo del conflicto de RUT incluyendo
  que restaurar nunca dispara solo.
- **[fix]** Nueve hallazgos de revisión sobre la misma card, todos con prueba:
    - **Aislamiento por filial:** el servicio mock guardaba un único array global de proveedores;
      dos filiales veían y pisaban los datos de la otra. Ahora particiona el store y el log de
      `Idempotency-Key` por `subsidiaryId`, con las seis funciones recibiéndolo como primer
      argumento.
    - **Respuesta tardía de la ficha:** una `fetchProcurementSupplierDetail` lenta podía resolver
      después de una más reciente y pisarla. El slice guarda `currentRequestId`/`listRequestId`
      (propiedad de contexto, ZF-12) y descarta cualquier `fulfilled`/`rejected` que no sea el de
      la última petición pedida.
    - **`deactivate` dependía de un cuerpo fuera de contrato:** el mock devolvía la ficha
      completa en un `DELETE`, que el contrato define como `204` sin cuerpo — a diferencia de
      `restore`, que sí la devuelve. El servicio ahora resuelve `void`; el slice sólo puede marcar
      `is_active` con certeza (no `allowed_actions`, que no adivina) y la ficha se refresca
      explícitamente tras desactivar.
    - **Restaurar sin `Idempotency-Key` ni autorización:** los tres lugares que restauran (fila,
      ficha, banner de conflicto) disparaban el thunk sin clave, y el botón del banner no pasaba
      por `ProtectedButton`. Unificados en `useSupplierRestore` (clave propia por intento) y el
      botón ahora exige `restore-procurement-supplier`.
    - **La clave no se renovaba tras un error definitivo:** un 409/422 dejaba la misma
      `Idempotency-Key` para el siguiente intento con datos corregidos, lo que el propio mock
      rechazaría como `IDEMPOTENCY_KEY_REUSED`. El conflicto de RUT pasó a un estado local
      (`conflict`) para poder renovar la clave sin apagar el banner que la usa.
    - **Formulario:** el alta no se reseteaba tras crear con éxito (Formik no detecta que
      `initialValues` "cambió" si sigue siendo el mismo objeto vacío); giro, teléfono y ambas
      direcciones validaban en silencio, sin `invalidFeedback` visible.
    - **Menú:** el gate del ítem padre «Inventario» no incluía el permiso de proveedores, así que
      un usuario con sólo `view-procurement-supplier` no veía el módulo en el menú aunque la ruta
      le funcionara por URL directa.
- **[test]** 45 pruebas nuevas/actualizadas para esta ronda: aislamiento entre filiales,
  desactivación 204 sin cuerpo, respuesta tardía descartada, renovación de clave tras conflicto,
  `useSupplierRestore` con clave propia por intento.
- **Estado:** Implementación terminada y verificada localmente (`tsc`, `lint:changed`,
  `prettier:check`, `check:eol`, `lint:imports`, 98 archivos / 701 pruebas relacionadas, todo en
  verde). Falta publicación si se autoriza.

---

## Tema: ZF-108 — Documentos de compra (boleta y factura)

**Rama:** `feat/documentos-compra`, sobre `feat/maestro-proveedores` (ZF-107, `#196`, aún no
fusionado). Tercera card del proyecto «refactor + expansión stock», bloqueada por ZF-107 y
bloqueante de ZF-109 (adjuntos) y ZF-110 (recepciones).

Mockear el documento de compra —lo que declaró la boleta o la factura— como entidad separada de
la recepción física (sección 6 del contrato): listado con filtros, alta y edición en borrador con
líneas, confirmación, cancelación y lectura de cobertura por línea. Sigue siendo mock — ningún
endpoint existe —, y es el primero del módulo que necesita **concurrencia optimista** además de
idempotencia: la edición exige `ETag`/`If-Match`, infraestructura que ZF-106 ya había construido
sin usar todavía (`buildProcurementWriteHeaders`, `readEtagHeader`, `useIdempotentWrite`).

- **[feat]** Tipos de la sección 6 en `procurement.interface.ts`: estados documentales, cobertura
  de recepción (`reception_status`, `null` en `draft`/`cancelled`), línea con
  `accounted_quantity`/`remaining_quantity`/`received_distribution`, fila resumida sin `items` ni
  `supplier_snapshot`, ficha completa, payloads de alta/edición (reemplazo total de `items`) y de
  cancelación.
- **[feat]** Cuatro fixtures en `procurement.db.ts`, derivados de los ejemplos literales del
  contrato: factura confirmada y **parcialmente recibida** (mismos IDs 24/101/1234 que el ejemplo
  de `inventory-stock/origins`, con una recepción posted que bloquea `cancel`), boleta en
  `draft` sin proveedor, factura en `draft` con dos líneas del mismo producto a costos distintos,
  y factura anulada con motivo. Se agregó un segundo producto no serializado activo (teclado):
  el único que había (`cableProduct`) está inactivo.
- **[feat]** Servicio mock en `services/procurement/purchaseDocuments.service.ts`: seis rutas más
  las dos listas relacionadas paginadas (siempre vacías — nada las puebla hasta las cards 04/05,
  pero paginan de verdad). Reutiliza `getProcurementSupplier` del servicio de proveedores (mismo
  store particionado por filial) para resolver el compacto y fijar `supplier_snapshot` al
  confirmar. `ETag` por documento con versión en memoria; `PATCH` sin `If-Match` es `428`, con uno
  obsoleto `412 RESOURCE_VERSION_CONFLICT`.
- **[feat]** Slice `store/slices/procurement/purchaseDocumentsSlice.ts`: guarda `currentEtag`
  junto a `current` — la única ficha del módulo que lo necesita, para el `If-Match` del próximo
  `PATCH`.
- **[feat]** Pantallas en `pages/inventario/abastecimiento/DocumentosCompra/`: listado (filtros de
  folio/proveedor/RUT, tipo, estado, cobertura y rango de emisión) y `DocumentoCompraDetalle/`
  (líneas con `CostBlock` y cobertura, snapshot de proveedor, `related_counts`, botonera por
  `allowed_actions`). Formulario único de alta/edición con `FieldArray` de líneas
  (`SelectReact` + `CostInput` reusados de ZF-106). Rutas y menú con `view-purchase-document`
  (sección 15).
- **Decisión:** El costo efectivo de una línea lo decide el **tipo documental**, no la base que
  digitó el usuario — factura → neto, boleta → bruto, sección 2 del contrato —, nunca la base
  ingresada. Los fixtures `grossEnteredCost`/`netEnteredCost` de ZF-106 ya cubrían literalmente
  ambos casos; el servicio los reproduce con la misma aritmética decimal sin float.
- **Decisión:** `create_receipt` y `add_attachment` se omiten a propósito de todo
  `allowed_actions` que genera este mock. Ofrecerlos llevaría a un botón sin pantalla detrás —
  esas dos acciones pertenecen a las cards 05 y 04, que bloquea esta card, no al revés.
- **Decisión:** Los códigos de error propios del mock (`PURCHASE_DOCUMENT_ITEMS_EMPTY`,
  `PURCHASE_DOCUMENT_HAS_ACTIVE_RECEIPTS`, `PURCHASE_DOCUMENT_NOT_DRAFT`) **no** se registraron en
  `PROCUREMENT_ERROR_DEFINITIONS`: esa tabla existe para copiar exactamente la sección 16 del
  contrato (una prueba de ZF-106 verifica el conteo), así que quedan como mensaje inline en el
  servicio — misma licencia que `PROCUREMENT_SUPPLIER_NOT_FOUND` en la card 02.
- **[test]** 29 pruebas nuevas en `purchaseDocuments.service.test.ts`: filtros y orden del
  listado, aislamiento por filial, factura sin proveedor (create y confirm), producto serializado
  rechazado, costo efectivo neto/bruto según tipo documental, idempotencia con reuso y con
  payload distinto, `428`/`412`/`409 PURCHASE_DOCUMENT_IMMUTABLE` de la edición, reemplazo total
  de `items` (ausente conserva, presente reemplaza), `DOCUMENT_LINE_MISMATCH`, confirmar con
  proveedor incompleto y con proveedor completo (snapshot fijado), anular sin motivo/ya
  anulado/con recepciones activas, y las listas relacionadas paginadas vacías con su 404.
- **[fix]** Autorrevisión con prueba: `supplier_id: ''` (boleta sin proveedor, el valor real que
  manda el formulario) casteaba a `NaN` en vez de a `null`/`undefined` — mismo defecto que
  `absentToUndefined` ya había resuelto en `costEntry.schema.ts` para el costo opcional, pasado
  por alto acá. La boleta sin proveedor rechazaba con el mensaje genérico de Yup en vez de
  guardar. Corregido con `.transform()` antes del cast; la prueba de
  `documentoCompraFormSchema.test.ts` falla al revertirlo.
- **[test]** 6 pruebas nuevas en `types.test.ts` sobre `documentoCompraFormSchema`: boleta válida
  sin proveedor, factura inválida sin proveedor y válida con uno, folio/fecha obligatorios,
  mínimo una línea, y cada línea exige producto/cantidad positiva/costo y base.
- **[fix]** Ronda de revisión sobre la misma card, ocho hallazgos:
    - `FieldArray` no tenía contexto Formik: el modal arma el formulario con el hook `useFormik`
      directo, no con el componente `<Formik>` que provee ese contexto automáticamente. Sin
      `<FormikProvider value={formik}>`, `push`/`remove` no encontraban `items` — agregar o quitar
      líneas no funcionaba en absoluto.
    - Propiedad de contexto (ZF-12) ausente en el slice: los IDs de documento se repiten entre
      filiales (cada una siembra los mismos fixtures), así que una escritura resuelta para la
      filial equivocada podía pisar `current` o parchear una fila del listado sólo porque el
      número de ID coincidía. Se agregó `listSubsidiaryId`/`currentSubsidiaryId` al estado y se
      comparan antes de aplicar cualquier mutación.
    - Los modales de la ficha (editar/confirmar/anular) no se cerraban al navegar de un documento
      a otro con el mismo componente montado: quedaban abiertos apuntando al documento anterior
      mientras `current` ya mostraba el nuevo.
    - El `412 RESOURCE_VERSION_CONFLICT` sólo mostraba un toast: no recargaba el documento ni
      bloqueaba el reenvío, así que el siguiente intento repetía el mismo 412 contra un ETag que
      nunca se refrescaba. Ahora el formulario se bloquea, muestra el conflicto inline y ofrece
      «Recargar», que pide el documento de nuevo antes de permitir editar otra vez.
    - El mock no serializaba escrituras concurrentes en dos sentidos: (1) la clave de idempotencia
      sólo se registraba **después** de terminar la operación, así que dos llamadas con la misma
      clave en vuelo se ejecutaban ambas en paralelo en vez de que la segunda viera
      `409 OPERATION_IN_PROGRESS`; (2) la validación de estado/ETag y la escritura final no eran
      un paso atómico, así que dos operaciones **distintas** sobre el mismo documento (confirmar
      y anular, por ejemplo) podían intercalarse entre la espera a `getProcurementSupplier` y el
      commit, y la que terminaba después pisaba en silencio a la que terminó primero — un
      documento recién anulado podía volver a `confirmed` sin que nadie lo pidiera. Se reservó la
      clave antes del primer `await` y se agregó una cola por documento
      (`withDocumentLock`) que serializa update/confirm/cancel.
    - `total_amount` no validaba que fuera un decimal real: un texto no numérico se serializaba
      como `null` en silencio, sin avisarle al usuario que lo que escribió no se guardó.
    - El detalle sólo mostraba los contadores de `related_counts`, no las listas paginadas de
      recepciones y asignaciones de stock inicial que pide la sección 6 («las listas relacionadas
      paginan; no se incrusta historia ilimitada»). Se agregó `RelatedListCard` +
      `useRelatedDocumentList`, paginando de verdad contra los dos endpoints ya construidos.
    - Dos campos (`total_amount`, `notas`) no mostraban `invalidFeedback`: un error de validación
      bloqueaba el envío sin que la UI dijera por qué.
- **[test]** 8 pruebas nuevas verificadas por mutación (revertido el fix del servicio con
  `git stash`, confirmado que las dos pruebas de concurrencia se ponen en rojo, restaurado):
  dos creaciones concurrentes con la misma clave (una gana, la otra `OPERATION_IN_PROGRESS`, sin
  duplicar el documento) y confirmar+anular en paralelo sobre el mismo borrador (se encolan; el
  estado final es el de la que terminó al final, nunca uno corrupto).
- **Estado:** Implementación terminada y verificada localmente (`tsc`, `lint:changed`,
  `prettier:check`, `check:eol`, `lint:imports`, 101 archivos / 793 pruebas relacionadas, todo en
  verde). Falta publicación si se autoriza.

---

## Tema: ZF-109 — Adjuntos privados del documento de compra

**Rama:** `feat/adjuntos-documentos-compra`, sobre `feat/documentos-compra` (ZF-108, `#198`, aún
no fusionado). Cuarta card del proyecto «refactor + expansión stock», bloqueada por ZF-108.

Mockear la gestión de archivos adjuntos de un documento de compra —subir, listar, descargar y
eliminar— sin exponer nunca una URL pública (subsección «Adjuntos privados» de la sección 6).
Va aparte de la card 03 porque el patrón de UI es distinto: la descarga pasa por una petición
autenticada, no por un enlace directo.

- **[feat]** Tipos en `procurement.interface.ts`: `IPurchaseDocumentAttachment`
  (`{id, file_name, mime_type, size, created_at}`, sin campo de URL en ningún punto), los tipos
  MIME aceptados y las constantes de límite (`PURCHASE_DOCUMENT_ATTACHMENT_MAX_SIZE_BYTES` = 10
  MB, `PURCHASE_DOCUMENT_ATTACHMENT_MAX_COUNT` = 10).
- **[feat]** Un adjunto semilla del documento confirmado `pcExpressInvoiceDocument` (id 24) en
  `procurement.db.ts`, con `related_counts.attachments` actualizado a 1 para que el fixture no se
  contradiga a sí mismo. El contrato no da un ejemplo de contenido binario, así que el mock
  sintetiza un `Blob` legible al servir la descarga en vez de dejar el archivo en `0 bytes`.
- **[feat]** Servicio mock en `services/procurement/purchaseDocumentAttachments.service.ts`: las
  cuatro rutas de la subsección, con la misma disciplina de `Idempotency-Key` y cola por
  documento (`withDocumentLock`) que ZF-108 introdujo para documentos — acá protege el cupo de 10
  contra dos subidas concurrentes que lean el mismo cupo disponible. Consulta el store de
  `purchaseDocuments.service` (nuevo export `findPurchaseDocumentForAttachments`) para el estado
  del documento en vez de mantener su propia copia, y lo actualiza (`setPurchaseDocumentAttachmentsCount`)
  tras cada escritura para que `related_counts.attachments` nunca quede desincronizado.
- **[feat]** `add_attachment` deja de omitirse en `allowed_actions`: ahora aparece en los
  fixtures y en las respuestas de crear/confirmar un documento `draft`/`confirmed` (nunca en
  `cancelled`), con los comentarios de ZF-108 que la excluían a propósito actualizados para
  reflejar que esta card ya existe.
- **[feat]** `DocumentAttachmentsCard` en `DocumentoCompraDetalle/components/parts/`: zona de
  subida con los límites visibles **antes** de intentar subir, lista con descarga autenticada
  (`URL.createObjectURL` sobre el `Blob` que devuelve el servicio, nunca un `<img src>`) y baja
  sólo en `draft`. Vive siempre visible en el detalle —no es un modal—, así que el botón
  «Adjuntar» de `AllowedActionsToolbar` sólo enfoca su selector de archivos vía un
  `useImperativeHandle` expuesto por la card.
- **[feat]** `useDocumentAttachments`: estado local del componente (mismo criterio que
  `useDeferredPaymentAttachments` en pagos diferidos — los adjuntos sólo importan mientras la
  ficha está abierta), sube secuencialmente (cada subida valida el cupo vigente, que un
  `Promise.all` en paralelo podría leer desactualizado) y avisa al padre (`onChanged`) para que
  recargue el documento completo en vez de sincronizar dos stores a mano.
- **Decisión:** Sin URL pública en ningún tipo ni en ningún punto del mock — ni el fixture, ni el
  servicio, ni el estado del hook construyen o guardan una. La descarga siempre resuelve
  `{blob, fileName, mimeType}` de una función de servicio.
- **Decisión:** Los códigos de error propios de esta card
  (`PURCHASE_DOCUMENT_ATTACHMENT_NOT_ALLOWED`, `_TYPE_INVALID`, `_TOO_LARGE`,
  `_QUOTA_EXCEEDED`, `_DELETE_NOT_DRAFT`) no se registraron en `PROCUREMENT_ERROR_DEFINITIONS` —
  la sección 16 no lista un código estable para «no se puede adjuntar en este estado» y esa tabla
  existe para copiar exactamente la sección 16, misma licencia que los códigos propios de ZF-108.
- **[test]** 28 pruebas nuevas: 17 en `purchaseDocumentAttachments.service.test.ts` (tipo/tamaño/
  cupo, estados permitidos por acción, ausencia de URL pública, aislamiento por filial, cola por
  documento verificada con dos subidas concurrentes contra el cupo justo, idempotencia) y 11 en
  `useDocumentAttachments.test.tsx` (rechazo en cliente antes de golpear el servicio, ciclo subir/
  descargar/eliminar, guards de `canUpload`/`canDelete`, tanda con fallo a mitad de camino).
- **[fix]** Autorrevisión, dos hallazgos propios verificados por mutación (revertida cada línea,
  confirmada la prueba en rojo, restaurada):
    - `refetch()` intentaba forzar la recarga de la lista volviendo a la página 1, pero si ya
      estaba en la página 1 `setPage(1)` era un no-op — React descarta un `setState` con el mismo
      valor — y una subida o baja exitosa no refrescaba la lista. Se agregó un `reloadToken` que
      se incrementa en cada `refetch()` y entra en las dependencias del efecto de carga, sin
      depender de que la página cambie.
    - Si una tanda de varios archivos fallaba a mitad de camino (por ejemplo, otra pestaña agotó
      el cupo entre el chequeo del cliente y la subida), el `catch` no refrescaba la lista: los
      archivos anteriores al que falló ya estaban guardados de verdad en el store mock, pero la
      UI seguía sin mostrarlos hasta un refresco manual. `refetch()`/`onChanged()` se movieron a
      un `finally`, para que corran también sobre un éxito parcial.
- **[test]** Prueba nueva para el hallazgo del `finally`: mockea el segundo archivo de una tanda
  de dos con un 422 del servidor y verifica que el primero —ya subido— queda visible igual.
- **Estado:** Implementación terminada y verificada localmente (`tsc`, `lint:changed`,
  `prettier:check`, `git diff --check`, pruebas relacionadas del módulo, todo en verde). Falta
  publicación si se autoriza.

---

## Tema: ZF-101 — Los iconos de las tarjetas de puertos no se dibujan

**Rama:** `fix/iconos-tarjetas-puertos` (desde `develop`)

- **[fix]** Los nueve nombres de `PORT_COUNTER_VISUALS` (`ports.visuals.ts`) no cumplían ninguna de las tres formas que `resolveIconLoader` sabe resolver, así que las tarjetas de «Cantidad de Puertos» de aio, docking y monitor se dibujaban sin icono y sin error en consola. Ahora apuntan a archivos reales de `@/components/icon/duotone`, con el prefijo `Duo`: `DuoDisplay1` (VGA), `DuoTv1` (HDMI), `DuoDisplay2` (DisplayPort), `DuoDisplay3` (DVI), `DuoUsb` (USB-A), `DuoUsbStorage` (USB-C), `DuoSdCard` (lector SD), `DuoRouter1` (RJ45) y `DuoBatteryCharging` (carga). Se eligió una única familia para que la grilla no mezcle estilos.
- **[fix]** El barrido que pedía el alcance encontró tres iconos rotos más en el módulo, fuera del mapa de puertos: `HeroUsbCable` → `DuoUsb` en `MonitorAccessoriesSection` (hub USB), `AddUser` → `DuoAddUser` en `CrearLote` y `HeroClockSolid` → `HeroClock` en el vacío del historial de `traceability`. Los tres archivos existían; sólo faltaba el prefijo o el nombre exacto.
- **[refactor]** `PortCounterVisual.icon` pasa de `string` a `TDuotoneIcons`. Ésa es la barrera que faltaba: `Icon` recibe `TIcons`, que incluye `string`, así que `tsc` aceptaba cualquier nombre inventado. Con la unión duotone —generada desde los archivos de `duotone/`— un nombre inexistente ya no compila.
- **[test]** `constants/__tests__/portsVisuals.test.tsx` monta el `Icon` real (no un mock) con el nombre de cada tipo de puerto y exige que aparezca el `<svg>`; una prueba extra fija que el mapa cubra los nueve tipos del catálogo.
- **Decisión:** no se toca `Icon.tsx` ni `TIcons`. Quitar `| string` de `TIcons` es un cambio global, con consumidores en todo el repositorio, y excede el alcance de la card; el tipado del mapa cierra el defecto donde ocurrió.
- **Aclaración sobre el criterio de cierre:** la card habla de «las nueve tarjetas en los cinco formularios», pero la grilla con iconos existe en tres (aio, docking y monitor). Desktop y notebook arman su grilla desde el mismo catálogo con un diseño sin iconos, así que no había nada que corregir ahí.
- **Verificación:** `typecheck` en verde; `test:related` sobre los archivos tocados, 7 suites / 189 pruebas (179 antes, +10 nuevas); `check:eol` y `lint:imports` limpios; `lint:changed` 95 errores, exactamente los mismos 95 que arroja la versión de `develop` de esos archivos (deuda preexistente de `any`, 0 hallazgos nuevos); `git diff --check` limpio.
- **Verificación de las pruebas (defecto recurrente 4):** con `vga` de vuelta en `'Video'`, `portsVisuals` queda en rojo (1 fallo / 9 pases) y `tsc` reporta `TS2322`; con la corrección, ambos en verde.
- **[fix]** `prettier:check` marcaba `bitacora_trabajo/04-09-2026.md` por una línea en blanco faltante antes de un `## Tema:`. Venía roto desde `develop`, así que se corrige acá para que el `Calidad de PR` de este PR corra en verde. Es la misma línea que arregla `da39273d` en `fix/pagos-diferidos-fecha-emision`; al ser idéntica, git no la va a poner en conflicto cuando esa rama se fusione.
- **Estado:** publicado. El usuario autorizó commit, push y creación del PR.
- ZF-102 — Quinta pasada de revisión del PR #190 y corrección del rol ARIA de `YesNoSelector`

---

## Tema: ZF-102 — Quinta pasada de revisión del PR #190 y corrección del rol ARIA de `YesNoSelector`

**Rama:** `fix/zf-102-cierre-revision-notebook-y-aio` (PR #190)

- **Revisión (5.ª pasada):** los cuatro hallazgos de las pasadas anteriores siguen cerrados sobre la
  punta `79ba244e` (`shouldValidate: true` en ambos `setValue`, `resolveNotebookSchema`, resolver
  directo sin `useRef`/`useCallback`, prueba de llegada asíncrona del schema). Las cifras del cuerpo
  del PR se sostienen exactamente: 122 suites / 872 pruebas, +1335 / −31 en 25 archivos.
- **Bloqueante encontrado:** el PR introduce `role='radiogroup'` + `aria-required` en
  `ui/YesNoSelector.tsx`. Era correcto cuando se escribió —`SelectionCard` exponía `role='radio'`—,
  pero el merge `9c5cf605` trajo #189, que la convirtió en botón de alternancia
  (`<button aria-pressed>`). El resultado combinado es un `radiogroup` sin un solo hijo
  `role='radio'`: ARIA inválido, en un componente compartido con **ocho consumidores**. El cuerpo
  del PR lo declaraba como deuda ajena, pero la línea que lo causa está en su propio diff.
- **[fix]** `YesNoSelector` pasa a `role='group'` y deja de emitir `aria-required`, inválido en ese
  rol. La obligatoriedad se sigue anunciando por el asterisco del rótulo al que apunta
  `aria-labelledby`. Es el criterio que #189 aplicó a los grupos de `InputSection` y `ScreenSection`
  y que `DockingExtrasSection` ya documenta para el bloque del candado.
- **[fix]** Se corrigen dos comentarios que afirmaban lo contrario del código: el del cuerpo del
  componente («Las dos tarjetas son `role="radio"`») y el JSDoc de la prop `required`, que prometía
  un `aria-required` que ya no existe.
- **[test]** Realineadas las siete consultas de `zf102CompletionFields.test.tsx` de
  `getByRole('radiogroup')` a `getByRole('group')`. Las dos aserciones de `aria-required` pasan a
  `toHaveAccessibleName('¿Enciende? *')` y `toHaveAccessibleName('¿El equipo enciende? *')`: la
  obligatoriedad ahora viaja en el nombre accesible, así que se verifica donde realmente está.
- **Defecto recurrente #4 verificado:** restaurar `role='radiogroup'` con su `aria-required` deja
  **8 pruebas en rojo** de `zf102CompletionFields.test.tsx`. Mutación revertida.
- **[chore]** Merge `79868710` de `origin/develop` (`1de64153`, con #193). Único conflicto:
  `bitacora_trabajo/07-09-2026.md`, resuelto por unión del índice y de los bloques ZF-102 y ZF-105.
  El código mergea limpio. El commit `79ba244e` (separador de ZF-99 en `04-09-2026.md`) quedó
  redundante porque #193 llevó la misma corrección a `develop`; converge sin conflicto, tal como se
  había previsto.
- **Verificación:** `typecheck` 0 errores; `test:run` 122 suites / 876 pruebas en verde sobre el
  merge; `test:related` del módulo 21 suites / 288 pruebas; `prettier:check`, `check:eol`,
  `lint:imports` y `git diff --check` limpios; `eslint` sobre los dos archivos tocados sin hallazgos
  nuevos.
- **Estado:** correcciones aplicadas en la rama del PR. Falta actualizar el cuerpo del PR, que
  todavía declara el `radiogroup` como deuda ajena. Commit, push y edición del PR pendientes de
  autorización.

---

## Tema: Atajo «Equipo NO ENCIENDE» inutilizable en desktop

**Rama:** `fix/atajo-no-enciende-desktop` (desde `develop`)

- **[fix]** El atajo «Equipo NO ENCIENDE» de un desktop dejaba la revisión trabada: guardaba los
  detalles y el cierre moría con «Debes completar si el equipo enciende antes de finalizar la
  revisión». `powers_on` es el único campo que `COMPLETION_REQUIREMENTS` exige para cerrar un
  desktop y vive en la sección Estética, a la que el atajo nunca llega porque cierra de una sola
  pasada; el payload de `DesktopHardwareSection` no lo respondía, así que llegaba `undefined` y el
  filtro de `filterTechnicalReviewPayload` lo dejaba fuera del PATCH. Ahora envía
  `powers_on: false`, que además es el dato real del atajo.
- **Alcance verificado contra el backend:** los otros cuatro atajos ya cumplen sus requisitos de
  cierre. Notebook responde los cinco campos que le exige el backend (ZF-102); aio y monitor sólo
  deben `dead_pixels_count`/`spots_count` cuando `screen_condition` los pide, y ambos envían la
  pantalla en `broken` con los contadores en 0. Desktop era el único hueco: aio y monitor ni
  siquiera declaran `powers_on` en sus reglas.
- **Decisión:** corrección de una línea en el payload, sin refactor del contrato de los atajos. Los
  cinco payloads siguen siendo literales por sección, que es el patrón vigente del módulo.
- **Deuda declarada:** `ScreenBypassPayload.test.tsx` cubre los atajos de monitor y aio, y
  `zf102CompletionFields.test.tsx` el de notebook; el de desktop sigue sin prueba de regresión.
  Queda a criterio del usuario si se agrega.
- **Verificación:** `test:run` 123 suites / 886 pruebas en verde; `typecheck` sin errores;
  `check:eol`, `lint:imports` y `prettier:check` limpios; `eslint` sobre el archivo tocado con 0
  errores y las mismas 4 advertencias preexistentes de `develop`.
- **Estado:** commit y PR pendientes de autorización.

---

## Tema: Preparación del release v2.4.0 y promoción de `develop` a `main`

**Rama:** `chore/preparar-v2-4-0` (desde `develop`)

- **[chore]** `package.json` sube de `2.3.1` a `2.4.0`. Los 17 commits acumulados desde el release anterior incluyen funcionalidades nuevas —campos estructurados de revisión técnica, estado del candado en docking, selector de clientes compartido, columnas de emisión y pago en cobranza y la primera card de Retiros de Equipos— sin cambios incompatibles conocidos, así que corresponde un incremento menor.
- **[docs]** `Docs/releases/v2.4.0.md` con el detalle por módulo, la sección de calidad y compatibilidad —cambio de rol ARIA de `SelectionCard`, contrato de puertos de lista a mapa `{tipo: cantidad}`, opt-in de `releasesOnSubsidiaryChange`— y la deuda conocida no bloqueante.
- **[docs]** Bloque de la versión en `RELEASE_NOTES.md` y actualización del encabezado «Release Actual» del `README.md`.
- **Verificación:** `test:run` 123 suites / 888 pruebas en verde; `typecheck` sin errores; `prettier:check`, `lint:imports` y `git diff --check` limpios.
- **Nota de entorno:** `lint:imports` arrancó con 5.113 errores de `import/extensions` por `node_modules` desestacado; `pnpm install --frozen-lockfile` lo dejó en 0. `check:eol` marca `.env` con CRLF en el árbol de trabajo local, no en el índice: el archivo está versionado y en CI el checkout limpio pasa.
- **Estado:** pendiente de autorización para push y creación del PR de release contra `main`.
