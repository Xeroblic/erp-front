# 16-09-2026

## Temas del día

- Documento de compra: formato peso en los montos y edición del proveedor seleccionado.
- Listas desplegables ilegibles en modo claro (`color-scheme`).
- Ajustes y traslados: página más fácil de entender (pasos, lenguaje llano, buscador de productos).

---

## Tema: Documento de compra — formato peso y edición de proveedor

**Rama:** `feat/documento-compra-formato-peso-y-proveedor` (desde `feat/refactor-expansion-stock`)

- **[feat]** Total informativo, costo de cada línea y costo de envío se muestran con formato peso mientras se escribe (`$1.234.567`, `$1.234,5`). Formik conserva el valor sin formato, así que validación, totales calculados y payload no cambian. Helpers `parsePesoInput` / `formatPesoInput` en `procurementDecimal.util.ts`.
- **Decisión:** el punto es separador de miles (es-CL); el decimal se escribe con coma y se limitan dos decimales. Al editar, el decimal del contrato (`"5712.00"`) se presenta como `$5.712,00`.
- **[feat]** Botón «Editar proveedor» junto a «Nuevo proveedor», visible con un proveedor elegido y permiso `edit-procurement-supplier` (`scope: 'access'`). Evita salir a la ficha para completar giro o direcciones antes de facturar. `useSupplierInlineEdit` pide la ficha completa (el `Select` sólo tiene la fila resumida) y sólo abre si el servidor ofrece `update`; al guardar, el proveedor actualizado reemplaza la opción sin perder el borrador.
- **[test]** `procurementDecimal.util.test.ts` (7) y `useSupplierInlineEdit.test.tsx` (5).
- **Estado:** commit y PR hacia `feat/refactor-expansion-stock`.

---

## Tema: Opciones de listas desplegables en blanco (modo claro)

**Rama:** `feat/documento-compra-formato-peso-y-proveedor`

- **[fix]** En Ajustes y traslados, al pasar el mouse por las opciones de un `<select>` todo se veía blanco en modo claro. Causa: `index.html` declara `color-scheme: dark light`; con el sistema operativo en oscuro, el navegador pintaba los controles nativos en oscuro aunque la app estuviera en claro. `index.css` fija `color-scheme` según la clase `dark` de la app.
- **[fix]** `Select` compartido: colores propios de las opciones sólo en modo oscuro. En claro se dejan los del navegador, porque un fondo de autor en `option` rompe el resaltado al pasar el mouse.
- **Verificado:** estilos computados en la app real en ambos modos (`color-scheme` light/dark según el tema). La lista nativa abierta no se puede capturar; queda pendiente confirmarla a mano.

---

## Tema: Ajustes y traslados — claridad de la página

**Rama:** `refactor/claridad-ajustes-traslados` (desde `feat/refactor-expansion-stock`)

- **[refactor]** Cada pestaña es un asistente paso a paso con el formato de `FormShell` de revisiones técnicas (`StepWizard` + `useStepWizard`): barra de pasos con íconos, un paso visible, Anterior / progreso / Siguiente y botón final en verde. Pasos: ubicación u origen y destino → productos → motivo y confirmación. «Siguiente» valida sólo el paso actual (`validateStep` en cada hook); al confirmar se revisan los pasos anteriores. Tras aplicar, la tarjeta de resultado reemplaza al asistente (no vuelve a «Productos»); «Nuevo ajuste» / «Nuevo traslado» reinicia desde el paso 1 conservando la ubicación, a pedido del usuario. Títulos con tono profesional, sin preguntas, a pedido del usuario.
- **[refactor]** Copy en lenguaje llano: «Diferencia» → «Sumar o restar», «Saldo actual» → «Stock actual», «Procedencia/FIFO automático» → «Descontar de / Automático (lo más antiguo primero)». Se acortaron los avisos largos y el aviso de compras quedó como «Otros movimientos de stock» (Recepciones y Transferencias). Se ajustaron los mensajes Yup del ajuste.
- **[refactor]** Tarjeta «Ajuste aplicado» más clara: frase por producto («Se restaron 2 unidades aptas de …»), tabla Cambio / Antes / Ahora de la condición ajustada más el total de la ubicación, y «Disponible para vender» (stock apto − reservado) con el faltante explicado en un aviso.
- **[feat]** Nueva columna «Quedará» en el ajuste: stock actual + cantidad, en rojo si queda negativo. En el traslado, la columna de saldo va antes de la cantidad.
- **[feat]** El selector de producto pasa a ser `SelectReact` con búsqueda por SKU o nombre, y no lista nada hasta que se escribe (`parts/productSearch.ts`). Payload y contrato sin cambios.
- **[fix]** Revisión del PR #218 (accesibilidad): al cambiar de paso el foco pasa al título del paso nuevo (antes quedaba en «Siguiente» o caía en `<body>` en el último paso), y cada asistente usa `useId()` para el título, porque ajuste y traslado quedan montados a la vez y repetían `id="wizard-step-title"`.
- **[test]** Tests recorren el asistente (`clickNext` espera la guarda anti doble clic de `Button`), reemplazan `SelectReact` por `<select>` nativo (patrón de `StockPorUbicacion`) y `framer-motion` por elementos planos. Nuevos: «Quedará», no avanzar sin ubicación/destino/cantidad válida, `productSearch.test.ts` y `useStepWizard.test.ts`. 28/28 en verde; typecheck y eslint limpios.
- **Decisión:** «Siguiente» no bloquea por el aviso local de saldo insuficiente (sólo por el esquema Yup); el rechazo del servicio sigue cubierto por los tests de 409.
- **Estado:** commit `d50d519a` (asistente); la tarjeta de resultado queda sin commit, pendiente de revisión visual.
